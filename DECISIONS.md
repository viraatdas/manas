# Decisions

Shared, agent-authored log of cross-cutting decisions the fleet must honor. The conductor records plan/steer decisions here; workers record interface contracts + adjustments. Re-read before each significant step.

## Plan approved
- **What:** Approved a 6-task plan for: Style direction: light mode, native macOS feel — think Things 3 meets a usage dashboard. Warm off-white surfaces, generous whitespace, one accent color doing all the work (a warm coral/amber suits "witnessing/attention" better than blue). Sentence case everywhere, SF Pro (system font), no gradients, no heavy shadows — flat cards with hairline borders like the rest of native macOS in 2026. Screen 1 — main popover (default view) Header row: ‹ Tue, Jul 14 › left-aligned, settings gear right-aligned. Below it, small muted metadata row: Last checked 2:14 PM · 3 sources synced Todo list (primary content): Each row: checkbox, task text, then an indented sub-row with the verdict chip + evidence, then Accept/Dismiss as ghost buttons Checked-off items collapse to strikethrough, no chip (settled, nothing to argue about) Add-todo field pinned above the list, not below Discovered activities (secondary section, visually quieter — surface-1 background, no border, slightly smaller text): "You might have also done this" — title, evidence line, Add / dismiss (x icon) Footer: token/usage strip (see Screen 2) + "Ask Claude" primary button, right-aligned Screen 2 — token/usage strip (lives in the footer, always visible) This is the "showcase how many tokens I consume" ask. Keep it compact — one line, expandable on click: [●●●○○] 2,140 tokens today · $0.03 · 4 checks A 5-dot or thin horizontal bar showing today's usage against a soft daily budget you set (visual, not alarming — muted gray/amber, not red) Click expands into a small breakdown card: tokens per check-in (timestamp, tokens in, tokens out, cost, which model), so you can see exactly which "Ask Claude" call was expensive and why This expanded view is where "what was done" lives too — a compact log: 2:14 PM — 3 todos judged, 1 discovered, 2,140 tokens Screen 3 — expanded usage view (click the strip) A slide-down panel, not a new window: Today's total — big number, tokens + cost, metric-card style Per-session table — time, tokens in/out, cost, model used, one-line summary of what it judged 7-day sparkline — tiny inline chart, just to see if usage is trending up as you rely on it more Model picker — Haiku (cheap, fast, default) vs Sonnet (better judgment, costlier) — since this is your dial to pull if quick-match keyword matching isn't cutting it but full Sonnet feels like overkill Design plugins/tools to actually build this SF Symbols for every icon (checkbox, chevrons, gear, x) — free, native, matches macOS exactly, no separate icon library needed SwiftUI + native Charts framework for the 7-day sparkline — no third-party charting dependency required Color: define one accent (Color("ManasAccent") in the asset catalog, coral/amber ~#D85A30-ish), everything else pulls from system colors (Color(.windowBackgroundColor), Color(.separatorColor)) so it auto-adapts if you ever add dark mode later Typography: stick to system text styles (.subheadline, .caption) rather than fixed point sizes — keeps it feeling native and respects the user's text size settings --- the idea of this mac application is that I want to be able to create my todo list it tells me how it's going in the day if i need to add and then also what else i seem to be working on. it takes a look at my claude and codex session in the day and then what i was working on as one source of input also if it's able to access granola that works too because i have meetings and stuff. sort of like the control panel of the day. I want it to be detailed type thing like should be able to tell me features i ws wokring on in my coding sessions. Tasks: Foundation: SPM scaffold, models, design system, persistence; Session ingestion: Claude Code + Codex activity readers; Judge engine: claude CLI verdicts, discovery, usage accounting; Screen 1: main window UI (todos, verdicts, discovered); Screens 2+3: usage strip and expanded usage panel; Integration: wire sources+judge into UI, end-to-end polish.
- **Why:** user-approved plan; workers implement these nodes in isolated workspaces, honoring the dependency edges
- **By:** conductor · 1784068217665

## n0: Foundation complete: SPM executable package (swift-tools-version 6.0, macOS 14+,
- **Did:** Foundation complete: SPM executable package (swift-tools-version 6.0, macOS 14+, zero third-party deps) with @main SwiftUI Window app (420x640, activates properly under swift run), light-mode design system, all core models, @Observable AppStore with debounced atomic JSON persistence to ~/Library/Application Support/Manas/state.json, protocol seams for sources/judge, and 18 passing tests. swift build, swift test, and app launch all verified.
- **Interfaces:** Package.swift (targets Manas + ManasTests); Sources/Manas/ManasApp.swift (@main, Window scene, AppStore via .environment — UI workers replace UI/ files but not this); Design/Theme.swift: Color.manasAccent #D85A30, .manasBackground #FAF8F5, .surface1 #F2EFE9, .hairline, View.manasCard(), ButtonStyle .ghost, Chip view, Verdict.Status.label/systemImage/tint; Models/: Todo, Verdict (status done|inProgress|notStarted|unknown, evidence, judgedAt, accepted: Bool?), DiscoveredActivity (resolution enum pending|added|dismissed), WorkSource enum claude|codex|granola (granola added per original request), WorkActivity (projectPath and endedAt optional), UsageRecord (totalTokens computed), CheckInDay, JudgeModel enum haiku|sonnet; Models/Protocols.swift: ActivitySource {name, fetchActivities(for:) async throws -> [WorkActivity]}, TodoJudge {judge(todos:activities:model:) async throws -> JudgeResult}, JudgeResult {verdicts: [UUID: Verdict], discovered, usage}; Store/AppStore.swift: @MainActor @Observable, persisted props autosave via didSet (debounced 500ms), helpers addTodo/toggleDone/removeTodo/setVerdictAccepted/dismissDiscovered/addDiscoveredToTodos/applyJudgeResult, aggregates records(on:)/tokensUsedToday/costTodayUSD/checkCountToday/checkInDays/recentDays(_:endingOn:), saveNow(), injectable init(fileURL:saveDebounce:), static makeEncoder/makeDecoder (ISO 8601). See DECISIONS.md for rationale.
- **Follow-ups:**
  - App bundle packaging [out of lane] — swift run works but a real .app bundle (Info.plist, icon) is needed for distribution and proper Dock identity
  - Wire saveNow() into app termination [out of lane] — debounced saves could theoretically lose the last <500ms of mutations on quit; an NSApplicationDelegate applicationWillTerminate hook would close that gap once real UI mutations exist
- **By:** n0 · 2026-07-14T22:41:31.604Z

## n0: foundation decisions (rationale)
- 2026-07-14 (n0 foundation): `WorkSource` enum is `claude | codex | granola` — spec said claude/codex, but the original request names Granola meetings as a source, so the case is there now (additive). It is shared by `DiscoveredActivity.source` and `WorkActivity.source`.
- 2026-07-14 (n0 foundation): `DiscoveredActivity` dismissed/added state is a single `resolution` enum (`pending | added | dismissed`) with `isAdded`/`isDismissed` conveniences, so an item can't be both.
- 2026-07-14 (n0 foundation): `WorkActivity.projectPath` and `.endedAt` are optional — meetings have no repo path and sessions may still be open.
- 2026-07-14 (n0 foundation): `AppStore` autosaves via property `didSet` → debounced (500 ms) atomic JSON write to `~/Library/Application Support/Manas/state.json` (ISO 8601 dates, pretty-printed). Mutate `store.todos[i]...` directly or use helpers (`addTodo`, `toggleDone`, `setVerdictAccepted`, `dismissDiscovered`, `addDiscoveredToTodos`, `applyJudgeResult`) — both persist. `saveNow()` forces a write; `init(fileURL:saveDebounce:)` is injectable for tests.
- 2026-07-14 (n0 foundation): model picker uses `JudgeModel` enum (`haiku | sonnet`, String raw values, default haiku). `TodoJudge.judge(model:)` takes a String per spec — pass `store.selectedModel.rawValue`.
- 2026-07-14 (n0 foundation): theme vocabulary lives in `Sources/Manas/Design/Theme.swift`: `Color.manasAccent` (#D85A30), `Color.manasBackground` (warm off-white window surface, #FAF8F5), `Color.surface1` (quieter warm surface, #F2EFE9), `Color.hairline`; helpers `View.manasCard()`, `.buttonStyle(.ghost)`, `Chip(text:systemImage:tint:)`, and `Verdict.Status.label/systemImage/tint` for verdict chips.
- 2026-07-14 (n0 foundation): package is swift-tools-version 6.0 (Swift 6 language mode, strict concurrency). `AppStore` is `@MainActor @Observable`; `ActivitySource`/`TodoJudge` are `Sendable`. UI workers replace files under `Sources/Manas/UI/` but keep `ManasApp.swift` as is (Window scene, `AppStore` injected via `.environment(store)`, read with `@Environment(AppStore.self)`).
## n4: usage UI interface contracts
- 2026-07-14 (n4 screens 2+3): `UsageStripView(isExpanded: Binding<Bool>)` is the compact footer strip (Screen 2); it only toggles the binding — it does not present the panel itself, so Screen 1 can lay it out next to the Ask Claude button. `UsageDetailPanel()` is the expanded view (Screen 3). `UsageFooterView` is a drop-in composite that owns the `isExpanded` state and slides the panel out above the strip (0.22s ease-in-out, clipped). All three read `AppStore` from the SwiftUI environment.
- 2026-07-14 (n4 screens 2+3): pure aggregation/formatting lives in `UsageMath` (Sources/Manas/UI/Usage/UsageMath.swift, tested): `totals(of:on:calendar:)`, `dailySeries(of:days:endingOn:calendar:)`, `filledDots(tokens:budget:dotCount:)` (proportional, rounds up, caps at 5), `isNearBudget(tokens:budget:threshold:)` (amber at ≥80%), `formattedTokens` ("2,140"), `formattedCost` ("$0.03", 3 decimals sub-cent), `modelDisplayName`. `dailySeries` intentionally mirrors `AppStore.recentDays` — the views use the pure version because it takes an injectable date/calendar; both agree, and AppStore (out of n4's lane) was not modified.
- 2026-07-14 (n4 screens 2+3): near-budget dot color is a muted amber defined fileprivate in UsageStripView.swift (not in Theme.swift, which stays single-accent). `UsageSampleData.store()` seeds a throwaway-file AppStore with 7 days of records (today = 2,140 tokens · $0.03 · 4 checks, matching the mock) for previews.

## n1: session ingestion decisions
- 2026-07-14 (n1 ingestion): `ActivityAggregator` (Sources/Manas/Ingestion/ActivityAggregator.swift) is the entry point for sync: `ActivityAggregator.standard.fetchActivities(for: Date) async -> AggregatedActivities` fans out to all sources concurrently and never throws. `AggregatedActivities` carries `activities` (sorted by startedAt), `syncedSourceCount` (sources that didn't throw — an empty day still counts as synced; feed this to `store.syncedSourceCount`), and `failedSourceNames`. Integration (n5) should construct `ActivityAggregator(sources:)` with a Granola source appended if/when one exists.
- 2026-07-14 (n1 ingestion): one `WorkActivity` per project per day, not per session file — sessions in the same cwd merge (time range spans, tokens sum, summary notes "· N sessions"). Feature-level detail lives in `features` (max 8 short phrases: Claude's `ai-title` / Codex's `task_complete` recap first, then sampled user prompts).
- 2026-07-14 (n1 ingestion): `WorkActivity.tokensUsed` is cache-inclusive total processed tokens (ccusage-style). Real sessions show 10M+; do not display it as if it were judge spend — the usage strip must keep using `UsageRecord`.
- 2026-07-14 (n1 ingestion): both sources take injectable `(directory: URL?, calendar: Calendar)` inits for tests; day boundaries use the injected calendar (default `.current`). File filter: only .jsonl files whose creation/mtime window overlaps the target day are opened (streamed line by line via `URL.lines`, unparseable lines skipped, 400-file backstop per fetch).

## n2: judge engine decisions (rationale)
- 2026-07-14 (n2 judge): `ClaudeCLIJudge` (Sources/Manas/Judge/) implements `TodoJudge` by shelling out to `claude -p <prompt> --output-format json --model <model>` — subscription auth, no API key, no SDK. Instantiate with defaults (`ClaudeCLIJudge()`); everything (runner, locator, timeout, clock) is injectable for tests.
- 2026-07-14 (n2 judge): verified the real CLI (v2.1.209) can emit its `--output-format json` envelope as a single result object OR an array of events ending in a `{"type":"result",...}` event (observed live), so `ClaudeCLIResponseParser` handles both plus newline-delimited events.
- 2026-07-14 (n2 judge): `UsageRecord.tokensIn` counts `input_tokens + cache_creation_input_tokens + cache_read_input_tokens` — cache tokens are still tokens the model processed, and the usage strip's job is to showcase real consumption. `costUSD` comes from `total_cost_usd`, 0 when absent (subscription). When the model's reply needs the one JSON-only retry, usage/cost accumulate across both calls.
- 2026-07-14 (n2 judge): CLI discovery order: `~/.claude/local/claude`, `~/.local/bin/claude` (where it actually lives on this machine), `/opt/homebrew/bin`, `/usr/local/bin`, then `$SHELL -l -c 'command -v claude'` — GUI apps don't inherit shell PATH. `ProcessCommandRunner` also prepends these dirs to the child PATH so the claude wrapper can find its runtime.
- 2026-07-14 (n2 judge): errors are typed as `JudgeError` (`cliNotFound`, `launchFailed`, `nonZeroExit`, `timedOut`, `malformedCLIOutput`, `malformedModelOutput`, `cliReportedError`) with sentence-case `errorDescription` for direct UI display. Timeout is 60s default; the child process gets SIGTERM then SIGKILL. Judge is async and cancellable, never touches the main thread.
- 2026-07-14 (n2 judge): discoveries carry an optional `source` field in the model's JSON schema (claude|codex|granola, defaults to `.claude` when absent) because `DiscoveredActivity.source` is required — additive to the spec's `{title, evidence}`.
- 2026-07-14 (n2 judge): empty todos AND empty activities short-circuits without a CLI call, returning a zero-token `UsageRecord` with summary "Nothing to judge".
- 2026-07-14 (n2 judge): integration smoke test against the real CLI is gated behind `MANAS_CLAUDE_INTEGRATION=1` (run once during development: passes, ~29s, haiku).

## n0: Resolved the 2-sided jj merge conflicts in .gitignore and
- **Did:** Resolved the 2-sided jj merge conflicts in .gitignore and DECISIONS.md. .gitignore: took the foundation worker's version wholesale since it was a strict superset of the other side (same Rudder ignore entries plus the required Swift/.build/.DS_Store/macOS sections). DECISIONS.md: merged both sides — kept the conductor's log (header, plan-approved entry, n0 completion report) and appended the worker's n0 rationale bullets as a new section titled 'n0: foundation decisions (rationale)', which the completion report already referenced. Verified: jj resolve --list is empty, swift build succeeds, all 18 tests pass.
- **Interfaces:** .gitignore (Rudder + SPM/Xcode + macOS sections); DECISIONS.md (conductor log + n0 rationale section merged)
- **By:** n0 · 2026-07-14T22:43:22.405Z

## n3: Screen 1 complete: replaced the placeholder ContentView with the
- **Did:** Screen 1 complete: replaced the placeholder ContentView with the full main window — header with ‹ Tue, Jul 14 › day navigation (forward chevron disabled at today) and gearshape settings popover (segmented Haiku/Sonnet picker + daily token budget field bound to AppStore), muted last-checked/sources-synced metadata row, add-todo field pinned above the scroll, judged todo list as one flat hairline card (accent checkbox, verdict chip + one-line evidence sub-row with Accept/Dismiss ghost buttons, checked items collapse to strikethrough with no chip, default animations), quieter surface-1 discovered-activities section (hidden when empty, Add ghost button + xmark dismiss), and a footer with hairline top border, usage-strip slot, and accent-filled Ask Claude button with ProgressView-while-running and inline caption error text. swift build passes, all 18 tests pass, app smoke-launched OK.
- **Interfaces:** Sources/Manas/UI/ContentView.swift: ContentView(judgeToday: (@MainActor () async throws -> Void)? = nil) — integration injects the judge pass here; Sources/Manas/UI/Main/{MainHeaderView,TodoList,DiscoveredSection,MainFooterView,PreviewData}.swift; MainFooterView.FooterUsagePlaceholder() is the single swap point for n4 UsageStripView(); AppStore.previewEmpty/.previewJudged/.previewWithDiscovered for previews; dismiss clears todo.verdict, accept uses store.setVerdictAccepted
- **Follow-ups:**
  - Swap FooterUsagePlaceholder for UsageStripView [out of lane] — n4 owns the real strip; the slot is one view name in MainFooterView.swift
  - Wire judgeToday through ManasApp/integration glue [out of lane] — ContentView takes the closure but ManasApp.swift (untouchable in my lane) still constructs ContentView() with nil
  - Per-day data when navigating dates [out of lane] — header selectedDate is local UI state; todo list and metadata do not yet filter by the selected day
- **By:** n3 · 2026-07-14T22:51:09.517Z

## n3: Screen 1 UI interface contract
- 2026-07-14 (n3 Screen 1): `ContentView` gained an injectable `judgeToday: (@MainActor () async throws -> Void)?` (default nil). Integration wires the real judge by constructing `ContentView(judgeToday: ...)`; while nil, tapping "Ask Claude" shows inline caption error "Claude judge isn't connected yet". While running it shows a small ProgressView and disables the button; thrown errors surface as inline `.caption` text via `error.localizedDescription`.
- 2026-07-14 (n3 Screen 1): the footer usage-strip slot is `FooterUsagePlaceholder()` in `Sources/Manas/UI/Main/MainFooterView.swift` — one view name in one place; integration swaps it for `UsageStripView()` when n4's view lands.
- 2026-07-14 (n3 Screen 1): dismissing a verdict clears `todo.verdict` entirely (judge can re-judge next pass); accepting uses `store.setVerdictAccepted(id, accepted: true)`. A verdict with `accepted == true` on an unchecked todo keeps its chip + evidence but loses the accept/dismiss buttons; `accepted == false` hides the sub-row (state unreachable from this UI).
- 2026-07-14 (n3 Screen 1): header date navigation is local `@State selectedDate` in ContentView (not persisted; AppStore has no such property) — forward chevron disabled at today. Integration can lift it if per-day fetching needs it.
- 2026-07-14 (n3 Screen 1): preview seeding lives in `Sources/Manas/UI/Main/PreviewData.swift` (`AppStore.previewEmpty/.previewJudged/.previewWithDiscovered`, temp-file-backed so previews never touch real state).
## n3: Resolved the 2-sided jj merge conflict in DECISIONS.md by
- **Did:** Resolved the 2-sided jj merge conflict in DECISIONS.md by keeping both sides: main already had n3's Screen 1 completion report, and the worker side added the n3 Screen 1 UI interface-contract rationale bullets. Kept the completion report in place and appended the interface-contract section after it, matching the pattern established by the earlier n0 conflict resolution. Verified jj resolve --list is empty, swift build succeeds, and all 18 tests pass.
- **Interfaces:** DECISIONS.md (n3 completion report + n3 Screen 1 UI interface contract section, both retained)
- **By:** n3 · 2026-07-14T22:52:44.935Z

## n4: Screens 2+3 complete in Sources/Manas/UI/Usage/: UsageStripView (5-dot soft-bud…
- **Did:** Screens 2+3 complete in Sources/Manas/UI/Usage/: UsageStripView (5-dot soft-budget gauge — muted gray, amber at >=80% — plus caption line like 2,140 tokens today · $0.03 · 4 checks; whole strip is a button toggling an isExpanded binding), UsageDetailPanel (today metric card, per-check-in table with time/model/in/out/cost/summary and alternating surface1 tint, 7-day Swift Charts BarMark sparkline in accent with narrow weekday labels, segmented Haiku/Sonnet picker bound to AppStore.selectedModel), UsageFooterView drop-in composite owning the slide-out animation, UsageSampleData seeded previews, and pure tested UsageMath aggregation. swift build and swift test pass (36 tests, 18 new). Views were visually verified by rendering to PNG via NSHostingView snapshots, which caught and fixed a clipped last-day sparkline label.
- **Interfaces:** UsageStripView(isExpanded: Binding<Bool>) — reads AppStore from environment, toggles binding only; UsageDetailPanel() — Screen 3 content, reads AppStore from environment, binds selectedModel; UsageFooterView — self-contained composite (Divider + panel slide-out above strip); UsageMath enum: totals(of:on:calendar:)->DayTotals{tokens,costUSD,checks}, dailySeries(of:days:endingOn:calendar:)->[CheckInDay], filledDots(tokens:budget:dotCount:), isNearBudget(tokens:budget:threshold:), formattedTokens(_:locale:), formattedCost(_:locale:), modelDisplayName(_:); UsageSampleData.store(now:) for previews. Consumed from n0: AppStore.usageRecords/dailyTokenBudget/selectedModel/records(on:), UsageRecord, CheckInDay, JudgeModel, Theme (manasCard, surface1, manasAccent, hairline).
- **Follow-ups:**
  - Wire UsageFooterView (or strip+panel) into Screen 1 footer [out of lane] — n3 owns ContentView/Screen 1; the strip is designed to sit left of the Ask Claude button with the panel above the footer — UsageFooterView is the zero-config option
  - Settings surface for dailyTokenBudget [out of lane] — AppStore.dailyTokenBudget drives the dots but nothing in the UI edits it yet; belongs with the settings gear
- **By:** n4 · 2026-07-14T22:58:39.142Z

## n1: Session ingestion layer complete: ClaudeCodeSource (parses ~/.claude/projects/*…
- **Did:** Session ingestion layer complete: ClaudeCodeSource (parses ~/.claude/projects/*/*.jsonl — user/assistant entries with timestamps, cwd, sidechain/meta filtering, per-message usage summing, ai-title/summary harvesting), CodexSource (parses ~/.codex/sessions/YYYY/MM/DD rollout files + flat legacy layout — session_meta/user_message/task_complete/token_count envelopes, AGENTS.md boilerplate filtering, prev-day dir scan for midnight-spanning sessions), and ActivityAggregator (concurrent fan-out, per-source failure tolerance, syncedSourceCount). Feature extraction turns prompts/titles into short human phrases with noise filtering (URLs, slash commands, ack replies). Fixtures modeled on real on-disk schemas from both CLIs. swift build + swift test pass: 40 tests (22 new). Verified against real transcripts: produces feature-level output like "Build SF Electric Company landing page with 3D model" per project.
- **Interfaces:** Sources/Manas/Ingestion/{TranscriptSupport,ClaudeCodeSource,CodexSource,ActivityAggregator}.swift; ClaudeCodeSource/CodexSource conform to ActivitySource with injectable (directory, calendar) inits; ActivityAggregator.standard.fetchActivities(for:) async -> AggregatedActivities {activities sorted by startedAt, syncedSourceCount, failedSourceNames}; one WorkActivity per project/day, features = max 8 phrases (ai-title / task_complete recap first), tokensUsed = cache-inclusive totals; DayWindow/TranscriptJSON/TranscriptFiles/SessionDigest/FeatureExtraction/ActivityBuilder internal helpers; tests in Tests/ManasTests/Ingestion/ incl. shared IngestionFixtures (UTC calendar, stamped file dates)
- **Follow-ups:**
  - Wire ActivityAggregator into AppStore sync flow [out of lane] — n5 integration needs to call ActivityAggregator.standard.fetchActivities and map syncedSourceCount/lastCheckedAt into the store for the metadata row
  - Granola meeting source [out of lane] — original request names Granola as a desired third source; WorkSource.granola case already exists and aggregator takes arbitrary source lists
  - Codex token totals after context compaction — token_count totals reset after compaction; high-water-mark keeps the max but slightly undercounts multi-compaction sessions
- **By:** n1 · 2026-07-14T22:58:52.601Z

## n1: Resolved the 2-sided jj merge conflict in DECISIONS.md by
- **Did:** Resolved the 2-sided jj merge conflict in DECISIONS.md by keeping both sides: main had appended the n4 usage-UI interface-contracts section while the n1 worker side appended the n1 session-ingestion decisions section at the same spot. Kept n4 first (matching main), then n1, following the additive-log pattern from the earlier n0 and n3 resolutions. Verified jj resolve --list is empty and swift test passes all 58 tests.
- **Interfaces:** DECISIONS.md (n4 usage UI interface contracts + n1 session ingestion decisions sections, both retained)
- **By:** n1 · 2026-07-14T23:00:32.345Z

## n2: Judge engine complete: ClaudeCLIJudge implements TodoJudge by shelling out
- **Did:** Judge engine complete: ClaudeCLIJudge implements TodoJudge by shelling out to the users claude CLI (subscription auth, no API key/SDK). Robust binary discovery (~/.claude/local, ~/.local/bin, homebrew, /usr/local/bin, login-shell fallback), 60s timeout with SIGTERM/SIGKILL, async+cancellable Process layer behind a CommandRunning seam. Parses all three CLI envelope shapes (single result object, event array, NDJSON — array shape verified live against claude 2.1.209), extracts usage (tokensIn includes cache creation+read) and total_cost_usd (0 on subscription). Judging prompt requests strict JSON verdicts {todoID,status,evidence} + discovered {title,evidence,source}; model-output parser strips fences, tolerates surrounding prose (brace-balanced scan), dict-keyed verdicts, alternate id keys, loose status spellings; one retry with a JSON-only nudge accumulating usage across both calls, then typed error. 55 new tests (73 total, all passing) incl. real-Process runner tests and an env-gated real-CLI integration smoke test (MANAS_CLAUDE_INTEGRATION=1, verified passing live).
- **Interfaces:** Sources/Manas/Judge/: ClaudeCLIJudge (struct, TodoJudge; init(runner:locator:timeout:now:) all defaulted); JudgeError enum (cliNotFound|launchFailed|nonZeroExit|timedOut|malformedCLIOutput|malformedModelOutput|cliReportedError, Equatable+LocalizedError with sentence-case messages for UI); CommandRunning protocol + CommandOutput + CommandError; ProcessCommandRunner (real Process, augmented PATH); ClaudeCLILocator; JudgePromptBuilder.build(todos:activities:); ClaudeCLIResponseParser/ClaudeCLIReply; JudgeOutputParser/ModelJudgeOutput. UsageRecord.summary format: "N todos judged, M discovered" (or "Nothing to judge" on empty short-circuit).
- **Follow-ups:**
  - Consider showing cache vs fresh token split in usage panel [out of lane] — tokensIn aggregates input+cache_creation+cache_read; the expanded usage view could distinguish them if users find totals surprisingly large
  - Surface JudgeError.errorDescription in Screen 1 when Ask Claude fails [out of lane] — errors are typed and UI-ready; integration node should render them (e.g. CLI missing banner)
- **By:** n2 · 2026-07-14T23:01:24.434Z

## n2: Resolved the 2-sided jj merge conflict in DECISIONS.md by
- **Did:** Resolved the 2-sided jj merge conflict in DECISIONS.md by keeping both sides, following the additive-log pattern from the n0/n3/n1 resolutions: kept main's n4 usage-UI and n1 session-ingestion decision sections in place (matching main's order), then appended the n2 worker's new section titled n2: judge engine decisions (rationale) directly after them. No content from either side was dropped. Verified jj resolve --list reports no conflicts and swift test passes all 113 tests (1 skipped env-gated integration test).
- **Interfaces:** DECISIONS.md (n4 + n1 sections retained, n2 judge engine rationale section appended)
- **By:** n2 · 2026-07-14T23:03:01.128Z

## followup: Codex token totals across context compaction
- 2026-07-14 (followup, Codex compaction): `SessionDigest.recordCumulativeTokens(_:)` (Sources/Manas/Ingestion/TranscriptSupport.swift) replaces the high-water-mark fold for Codex `token_count` events: cumulative totals restart low after each context compaction, so a drop banks the finished segment and `totalTokens` sums every segment. Monotonic sessions are unchanged (total = latest snapshot); repeated identical snapshots don't double count. `CodexSource` now calls this instead of `max`. ClaudeCodeSource's per-message `totalTokens +=` path is untouched.
- 2026-07-14 (followup, Codex compaction): my workspace predates n1's merge, so the nine Ingestion source/test files appear as adds here; six are byte-identical to what n1 landed on main, and only CodexSource.swift, TranscriptSupport.swift, and CodexSourceTests.swift differ (the fix + two new tests).
## followup: Resolved the 4-file jj merge conflict for the Codex
- **Did:** Resolved the 4-file jj merge conflict for the Codex compaction followup. The three Swift files were add/add conflicts where the agent side was a strict superset of what n1 landed on main (verified by diffing both parents): took the agent versions, which replace the high-water-mark token_count fold with SessionDigest.recordCumulativeTokens(_:) (banks each pre-compaction segment so multi-compaction sessions count in full) and add two tests. DECISIONS.md (3-sided): kept main's full log through the n2 sections and appended the followup section at the end, matching the additive-log pattern. Verified: jj resolve --list empty, swift build clean, swift test 115 tests / 0 failures / 1 skipped (env-gated).
- **Interfaces:** SessionDigest.recordCumulativeTokens(_:) in Sources/Manas/Ingestion/TranscriptSupport.swift; CodexSource token_count handling now calls it instead of max; DECISIONS.md followup section appended
- **By:** followup · 2026-07-14T23:10:16.533Z

## n5: Fully wired app, verified live end-to-end 5 times against
- **Did:** Fully wired app, verified live end-to-end 5 times against the real claude CLI. (1) Check-in engine at AppStore level: judgeToday() gathers today via ActivityAggregator.standard, judges via ClaudeCLIJudge with selectedModel, applies verdicts/discoveries/usage with dedupe; per user mid-task scope change there is NO Ask Claude button — checks run automatically (launch + hourly via startAutoCheckIns, manual via spinning arrow.clockwise header button), overlap-guarded, cancellable, errors as quiet footer caption. (2) Real UsageStripView + UsageDetailPanel composed into MainFooterView (strip full width, panel slides down); UsageFooterView + FooterUsagePlaceholder removed. (3) Persistence verified across multiple relaunches incl. usage history/sparkline; saveNow flushed on quit. (4) Design pass: resizable window (default 520x760, min 420x600, 720pt centered content column), custom JudgeModelPicker (native segmented picker is system-blue and ignores tint), hover states, springs, refined empty states, sentence case throughout. Fixed real bugs found live: FileHandle.bytes truncating CLI stdout at 64KB (root cause of flaky malformedCLIOutput; now blocking readToEnd + 300KB regression test), 60s judge timeout too tight (now 180s), usage strip wrapping under error text, hourly re-checks wiping accepted verdicts (same-status re-verdicts stay settled), empty-day checks skipping the CLI. swift build clean; swift test 130 tests 0 failures (5 skipped env-gated live/diagnostic). Live: 5 real check-ins (~28K tokens in / 3-10K out each, haiku) produced correct verdicts ("Water the plants" notStarted; wiring todo inProgress citing this session) and 13 real discovered activities; README rewritten.
- **Interfaces:** AppStore.judgeToday(aggregator:judge:) / checkInNow(...) / startAutoCheckIns(every:aggregator:judge:) / stopAutoCheckIns() / isCheckingIn / lastCheckInError (Sources/Manas/Store/AppStore+Judge.swift); AppStore.judgeTimeout=180s; applyJudgeResult now dedupes vs todos+within-pass and preserves accepted on same-status re-verdicts; JudgeModelPicker + HoverIconButtonStyle (.hoverIcon) + hover GhostButtonStyle in Theme.swift; ContentView.contentMaxWidth=720; ContentView()/MainFooterView() no longer take judgeToday; UsageFooterView deleted; ProcessCommandRunner.readToEnd now blocking-read based; env-gated diagnostics in Tests/ManasTests/PromptDumpTests.swift; live e2e test JudgeTodayLiveIntegrationTests (MANAS_CLAUDE_INTEGRATION=1)
- **Follow-ups:**
  - App bundle packaging [out of lane] — swift run works but a real .app (Info.plist, icon, Dock identity) is needed for daily use and login-item auto-start, which suits the auto-check model
  - Granola meeting source [out of lane] — WorkSource.granola and aggregator seams exist; original request names Granola but no reader was in any lane
  - Per-day view when navigating dates [out of lane] — header date navigation is still local UI state; todos/usage always show today (n3 noted the same gap)
  - Codex token totals after compaction [out of lane] — already tracked from n1; a sibling worker was on it during this task
- **By:** n5 · 2026-07-14T23:55:02.958Z

## n5: Fully wired app, verified live end-to-end with 6 real
- **Did:** Fully wired app, verified live end-to-end with 6 real claude-CLI check-ins today. (1) Check-in engine at AppStore level (Store/AppStore+Judge.swift): judgeToday() gathers today via ActivityAggregator.standard, judges via ClaudeCLIJudge with selectedModel, applies verdicts/discoveries/usage. Per the user mid-task scope change there is NO Ask Claude button — checks run automatically on launch + hourly (startAutoCheckIns, overlap-guarded Task loop), plus a spinning arrow.clockwise header button (checkInNow); errors surface as a quiet footer caption; header Last checked/sources synced is the heartbeat. (2) Real UsageStripView + UsageDetailPanel composed into MainFooterView (strip full width, panel slides down in-window); UsageFooterView + FooterUsagePlaceholder removed. (3) Persistence verified across many relaunches incl. usage history feeding the sparkline; saveNow flushed on willTerminate. (4) Design pass to Things-3 bar: resizable window (default 520x760, min 420x600, 720pt centered content column that scales gracefully when widened), custom JudgeModelPicker, hover states on rows/ghost/icon buttons, spring animations, refined empty states, 8pt rhythm, sentence case, SF Symbols, one coral accent. Five real bugs found by running it and fixed: FileHandle.bytes truncated CLI stdout at exactly 64KB (root cause of flaky malformedCLIOutput — now blocking readToEnd + 300KB regression test, verified live at 116KB); 60s judge timeout too tight for a real day (now 180s); usage strip wrapped under long error text; hourly re-checks wiped accepted verdicts (same-status re-verdicts now stay settled); hourly re-checks piled up rephrased duplicate discoveries (40 pending after 5 checks — pending set now refreshes per pass, added/dismissed kept forever; verified live 40 -> 4). Empty days skip the CLI entirely so auto-checks do not log zero-token records. swift build clean; swift test 131 tests, 0 failures (5 skipped env-gated live/diagnostic). Live evidence: verdicts cite real sessions (Water the plants -> notStarted; wiring todo -> inProgress citing the 4:03-5:16 PM session in this worktree), 4 real discovered activities across Claude+Codex, 210,501 tokens / $0.30 / 6 checks on the strip. README rewritten for automatic checking. NOTE: a dev build is still running from this worktree and will keep checking hourly (~$0.05/check); quit it from the Dock if unwanted.
- **Interfaces:** AppStore.judgeToday(aggregator:judge:) / checkInNow(...) / startAutoCheckIns(every:aggregator:judge:) / stopAutoCheckIns() / isCheckingIn / lastCheckInError (Sources/Manas/Store/AppStore+Judge.swift); AppStore.judgeTimeout=180s, autoCheckInterval=3600s; applyJudgeResult refreshes pending discoveries per pass (keeps added/dismissed forever), dedupes vs todo text + within-pass, preserves accepted on same-status re-verdicts; JudgeModelPicker + HoverIconButtonStyle (.hoverIcon) + hover-aware GhostButtonStyle in Design/Theme.swift; ContentView.contentMaxWidth=720; ContentView()/MainFooterView() take no judgeToday closure anymore; UI/Usage/UsageFooterView.swift deleted; ProcessCommandRunner.readToEnd is blocking-read based; env-gated diagnostics in Tests/ManasTests/PromptDumpTests.swift (MANAS_PROMPT_DUMP/MANAS_RUNNER_PROMPT/MANAS_PARSE_FILE); live e2e test JudgeTodayLiveIntegrationTests (MANAS_CLAUDE_INTEGRATION=1)
- **Follow-ups:**
  - App bundle packaging + login item [out of lane] — swift run works but a real .app (Info.plist, icon, Dock identity) plus launch-at-login is what the automatic hourly check model actually wants
  - Granola meeting source [out of lane] — WorkSource.granola and the aggregator source list already exist; the original request names Granola but no reader was in any lane
  - Per-day view when navigating dates [out of lane] — header date navigation is still local UI state; todos/usage always show today (n3 flagged the same gap)
  - Discovery churn across passes [out of lane] — pending discoveries now refresh per pass, so the set can visibly change wording between hourly checks; a stable per-activity identity (rather than title matching) would settle it
- **By:** n5 · 2026-07-15T00:19:47.383Z

## n5: integration decisions (rationale)
- 2026-07-14 (n5 integration): the "Ask Claude" flow is `AppStore.judgeToday(aggregator:judge:)` (Sources/Manas/Store/AppStore+Judge.swift), defaulting to `ActivityAggregator.standard` + `ClaudeCLIJudge(timeout: AppStore.judgeTimeout)`. ManasApp wires it via `ContentView(judgeToday: { try await store.judgeToday() })`. `syncedSourceCount` updates right after ingestion (even if judging then fails); `lastCheckedAt`/usage only on judge success.
- 2026-07-14 (n5 integration): judge timeout raised to 180s (`AppStore.judgeTimeout`) — a real day of transcripts produced a prompt haiku couldn't finish inside ClaudeCLIJudge's 60s default; observed timing out live, passes at ~60–90s.
- 2026-07-14 (n5 integration): `applyJudgeResult` now dedupes discoveries against existing todo texts and within-pass repeats (trimmed, case-insensitive), in addition to prior suggestions; blank titles are dropped.
- 2026-07-14 (n5 integration): `UsageFooterView` was removed — `MainFooterView` owns `isExpanded` itself so the strip sits left of the Ask Claude button and `UsageDetailPanel` slides out above the footer bar (same 0.22s ease-in-out). `FooterUsagePlaceholder` is gone; the strip gets `.fixedSize + .layoutPriority(1)` so error text truncates instead of wrapping the strip.
- 2026-07-14 (n5 integration): `MainFooterView` keeps the judge `Task` handle and cancels it in `onDisappear`; `CancellationError` is swallowed (window went away), other errors surface as the inline caption with a `.help` tooltip carrying the full message.
- 2026-07-14 (n5 integration): debounced saves are flushed on quit via `NSApplication.willTerminateNotification` → `store.saveNow()` in ManasApp (closes n0's follow-up).
- 2026-07-14 (n5 integration): live end-to-end pass is `JudgeTodayLiveIntegrationTests`, gated behind `MANAS_CLAUDE_INTEGRATION=1` like n2's smoke test (verified passing live, ~56s, haiku, real transcripts).
## n5: scope change — automatic check-ins, no Ask Claude button (user, mid-task)
- 2026-07-14 (n5 integration): the user removed the "Ask Claude" button mid-task. Checks now run automatically: `AppStore.startAutoCheckIns(every:aggregator:judge:)` (launch + hourly cadence via a Task loop, overlap-guarded), `AppStore.checkInNow(...)` behind a spinning `arrow.clockwise` header button, transient `isCheckingIn`/`lastCheckInError` observable state. The footer is the usage strip full-width plus a quiet error caption. Window is resizable: default 520×760, min 420×600, content capped at 720pt and centered when wider.
- 2026-07-14 (n5 integration): an empty day (no todos AND no observed activities) skips the CLI call entirely — auto-checks would otherwise pile up zero-token "Nothing to judge" records hourly. `lastCheckedAt` still updates so the header heartbeat stays honest.

## n5: 64KB pipe truncation bug in ProcessCommandRunner (root-caused live)
- 2026-07-14 (n5 integration): `FileHandle.bytes` silently truncated the claude CLI's stdout at 64KB (one capture was exactly 65536B, cut mid-JSON), which surfaced as flaky `malformedCLIOutput` once the CLI's event array outgrew the pipe buffer — passes worked in the morning and started failing as the day's transcripts grew. Fixed by draining with blocking `FileHandle.readToEnd()` on a background queue; regression test `testCapturesOutputLargerThanPipeBuffer` (300KB) added. Env-gated diagnostics kept in Tests/ManasTests/PromptDumpTests.swift (MANAS_PROMPT_DUMP / MANAS_RUNNER_PROMPT / MANAS_PARSE_FILE).
## n5: design-coherence fixes during the polish pass
- 2026-07-14 (n5 integration): native segmented `Picker` renders its selected segment in the SYSTEM accent (blue) on macOS and ignores `.tint` — replaced with `JudgeModelPicker` in Theme.swift (chip-language segmented control: accent text on accent-12% fill, surface1 track, hairline border) used by both SettingsPopover and UsageDetailPanel. Keeps the one-coral-accent law.
- 2026-07-14 (n5 integration): hover affordances live in Theme.swift — `GhostButtonStyle` gained a hover tint, new `HoverIconButtonStyle` (`.hoverIcon`) for header chevrons/refresh/gear and discovered-row dismiss. Todo rows highlight `primary.opacity(0.025)` on hover. Springs (`response 0.35 / damping 0.85`) on list changes and the usage-panel slide; check-off uses a symbol-replace transition.
- 2026-07-14 (n5 integration): AppDelegate now quits after the last window closes (single-window utility; previously a closed window left a headless process).
- 2026-07-14 (n5 integration): `applyJudgeResult` preserves `accepted == true` when a re-check returns the SAME verdict status (evidence refreshes); a changed status clears acceptance and resurfaces Accept/Dismiss. Without this, hourly auto-checks reset settled verdicts every pass (observed live).
- 2026-07-14 (n5 integration): pending discoveries are REFRESHED per pass, not accumulated. Every check re-observes the whole day and the model rephrases titles slightly, so exact-title dedupe missed and hourly auto-checks piled up near-duplicates (observed live: 40 pending items after 5 checks, burying the "quieter secondary" section). `applyJudgeResult` now keeps only added/dismissed items (forever, so they never reappear) and replaces the pending set with the latest pass's discoveries. A pending suggestion is the latest check's opinion, nothing more.
## icon: app icon decisions (rationale)
- 2026-07-15 (icon): the Manas app icon lives at `assets/icon/manas-1024.png` (1024×1024 RGBA master) and `assets/icon/Manas.icns` (iconutil-built, all ten standard slots: 16/32/64/128/256/512/1024 px covering @1x/@2x). Bundle packaging should point Info.plist `CFBundleIconFile` at Manas.icns.
- 2026-07-15 (icon): the mark is a bindu over a horizon — a solid coral disc (the mind as a point of attention; manas is Sanskrit for mind) above a coral capsule line (the day it witnesses). Chosen over a pure disc (reads as the Japan flag) and a ring-and-dot (reads as a record button). Exactly the app's palette: warm off-white #FAF8F5 tile, single coral #D85A30 mark, flat, hairline warm edge (#E8E3DB), one subtle baked-in shadow per Big-Sur convention (black 14%, y 7, blur 14) — no gradients.
- 2026-07-15 (icon): canvas follows the macOS Big-Sur-onward template: 1024 canvas, 824 pt continuous-corner squircle, transparent margins. The 16/32/64 px slots use a dedicated chunkier variant (bigger disc, 76 pt-equivalent horizon) because the master's 40 pt line vanished at 16 px — verified by rendering and inspecting every size.
- 2026-07-15 (icon): artwork is generated programmatically with a standalone SwiftUI `ImageRenderer` script checked in at `assets/icon/render-icon.swift` (run with `swift`, native toolchain only — outside all SPM target paths, so Package.swift and the build are untouched; verified it reproduces `manas-1024.png` byte-for-byte). Full rebuild pipeline (script → sips size ladder → `iconutil -c icns`) is documented in the script header; `manas-small-1024.png` is the chunkier small-slot master it also emits.

## n5: Resolved the 2-sided jj merge conflict in DECISIONS.md (the
- **Did:** Resolved the 2-sided jj merge conflict in DECISIONS.md (the only conflicted file). Main side held the Codex-compaction followup sections and both n5 completion reports; the n5 worker side appended four rationale sections (integration decisions, automatic check-ins scope change, 64KB pipe truncation bug, design-coherence fixes). Kept main content in place and appended the worker sections after it, dropping nothing from either side — same additive-log pattern as the n0/n3/n1/n2 resolutions. Verified: jj resolve --list empty, swift build clean, swift test 133 tests / 0 failures / 5 skipped (env-gated).
- **Interfaces:** DECISIONS.md (followup + n5 completion reports retained, n5 rationale sections appended)
- **By:** n5 · 2026-07-15T00:22:27.889Z

## worker: Built site/index.html — a single-file static landing page for
- **Did:** Built site/index.html — a single-file static landing page for Manas (no dependencies, inline CSS) matching the app design language: warm off-white #FAF8F5, coral accent #D85A30, system font stack, hairline borders, sentence case, one staggered CSS load animation honoring prefers-reduced-motion. Centerpiece is a pure-HTML/CSS miniature of the app window (todos with verdict chips + evidence, discovered activities, 5-dot usage strip) instead of a screenshot. Honest feature list incl. in-flight features (calendar, Arc/Screen Time, messages) with a Manas-is-in-active-development note. Deployed to Vercel project manas (scope viraatdas) rooted at site/, production. DNS: Netlify CNAME manas.viraat.dev -> cname.vercel-dns.com (record 6a56d53495ceb32ab4a37f2e, zone 68e08db53b1df70bdcfe7f4c), domain attached+verified on Vercel, TLS cert issued. Verified: https://manas.viraat.dev returns HTTP 200 with the page; visually checked via headless-Chrome screenshots at 1280/500px (one contrast fix applied: muted text darkened #8A8378->#756E63 for WCAG AA). No tokens committed: site/.gitignore covers .vercel and .env* (vercel link artifacts).
- **Interfaces:** site/index.html (whole page incl. inline CSS + typographic M mark placeholder for the app icon); site/.gitignore; Vercel project: viraatdas-projects/manas rooted at site/ (redeploy: cd site && vercel deploy --prod --yes); Netlify DNS record 6a56d53495ceb32ab4a37f2e in zone viraat.dev
- **Follow-ups:**
  - Swap the typographic M placeholder for the real app icon — assets/icon/manas-1024.png had not landed from the app-icon worker at deploy time; header/footer mark and favicon in site/index.html should use it once it exists, then redeploy
  - Add a download/install link when a distributable exists [out of lane] — The page has no call to action beyond reading — once a notarized DMG or GitHub release exists, link it from the hero
- **By:** worker · 2026-07-15T00:42:16.275Z

## 1784078316225825000-can-you-reverse-engineer-how-messages-app-works: Reverse-engineered Apple Messages storage against the real 830MB/689k-message c…
- **Did:** Reverse-engineered Apple Messages storage against the real 830MB/689k-message chat.db on this machine. Deliverables: docs/messages-app-teardown.md (findings + measured evidence) and Tools/messages-teardown/verify.py, a read-only self-verifying harness that re-derives all 20 claims from the live DB and exits non-zero when Apple drifts the schema (20/20 pass). Core finding: Messages does work at write time so reads are bounded index walks. The key trick is denormalizing message.date into chat_message_join.message_date so (chat_id,message_date,message_id) is a covering index - paging is 0.02ms vs 22ms for the obvious ORDER BY m.date (1000x, and a warm cache does NOT help since the naive path is CPU-bound sorting). Unread badge is a partial index of 4KB/178 rows against a 526MB table (134,000x smaller). Proved empirically that SQLite serial types 8/9 store 0/1 with zero payload, so booleans cost ~1 byte - which is why 55 flag columns is correct, not sloppy: it is what makes each flag indexable. Layout sizes persisted across launches as CKChatItemCachedSizeMetrics. HIGH-VALUE PRACTICAL FIND: message.text is being abandoned - NULL rate was ~0.2% for 2016-2025 then jumped to 68.2% in 2026; the current client only writes attributedBody (a NeXTSTEP typedstream, not NSKeyedArchiver). A SELECT text reader silently loses two-thirds of this years messages. Also: date is NANOseconds since the 2001 epoch. Scope-safe: touches only docs/, Tools/, .gitignore and DECISIONS.md - no Sources/, no Package.swift, so no conflict with the Messages-awareness feature node. swift build still clean.
- **Interfaces:** docs/messages-app-teardown.md (teardown + copy-paste-ready read query, itself covered by harness claim 15); Tools/messages-teardown/verify.py (standalone python3 stdlib, --db PATH, -v; exit 0 all claims hold / 1 a claim failed / 2 cannot read DB; not an SPM target so swift build/test are unaffected); DECISIONS.md entry Messages teardown: chat.db schema contract binding any iMessage reader (attributedBody is source of truth not text; date is ns since 2001; page via chat_message_join.message_date with a keyset cursor never OFFSET; open read-only, needs Full Disk Access).
- **Follow-ups:**
  - MessagesSource must read attributedBody, not text [out of lane] — The sibling Messages-awareness node will lose 68% of 2026 messages if it uses the conventional SELECT text approach. Contract + typedstream details are in DECISIONS.md and the teardown doc.
  - Apply the keyset/denormalized-sort-key lesson to Manas ingestion and the day-journal timeline [out of lane] — Manas sorts sessions/activities by time and the timeline node is building a scrollable multi-day list - both are the exact shape where a sort key on the wrong table or OFFSET paging reintroduces linear cost. Closing section of the teardown spells out the mapping.
  - Run verify.py after macOS updates [out of lane] — The 2026 text-column change proves Apple drifts this schema silently. The harness exists to catch the next one; it is not wired into CI because it needs a real TCC-protected chat.db.
- **By:** 1784078316225825000-can-you-reverse-engineer-how-messages-app-works · 2026-07-15T01:30:21.565Z

## Plan approved
- **What:** Approved a 2-task plan for: what ist he stauts of manas? lets fix it up and launch it so it's working. Tasks: App icon (1024px PNG + .icns); Package .app, install to /Applications, launch.
- **Why:** user-approved plan; workers implement these nodes in isolated workspaces, honoring the dependency edges
- **By:** conductor · 1784151899299

## icon: Manas app icon created: assets/icon/manas-1024.png (1024x1024 RGBA master) and
- **Did:** Manas app icon created: assets/icon/manas-1024.png (1024x1024 RGBA master) and assets/icon/Manas.icns (iconutil-built, all ten standard slots 16-1024px @1x/@2x, verified by round-tripping back to an iconset). Mark is a bindu over a horizon — solid coral #D85A30 disc above a coral capsule line on a warm off-white #FAF8F5 continuous-corner squircle (824pt on 1024 canvas), flat with a subtle Big-Sur-convention shadow and warm hairline edge; matches Theme.swift exactly. Three candidate marks were rendered and self-inspected at 1024/128/32/16px; the 16-64px icns slots use a checked-in chunkier variant (manas-small-1024.png) because the master horizon line vanished at 16px. Generator script checked in at assets/icon/render-icon.swift (standalone SwiftUI ImageRenderer, reproduces the master byte-for-byte; full sips+iconutil rebuild pipeline in its header). Zero package changes; swift build clean, swift test 133 tests 0 failures (5 env-gated skips). Rationale recorded in DECISIONS.md.
- **Interfaces:** assets/icon/manas-1024.png; assets/icon/Manas.icns; assets/icon/manas-small-1024.png (small-slot master); assets/icon/render-icon.swift (standalone regeneration script, outside SPM target paths); DECISIONS.md icon rationale section
- **Follow-ups:**
  - Point app bundle Info.plist CFBundleIconFile at Manas.icns [out of lane] — The icon only shows in Dock/Finder once the .app bundle packaging task wires CFBundleIconFile/CFBundleIconName to assets/icon/Manas.icns
- **By:** icon · 2026-07-15T21:51:35.193Z

## bundle: Manas is now a real installed app: scripts/make-app.sh reproducibly
- **Did:** Manas is now a real installed app: scripts/make-app.sh reproducibly builds dist/Manas.app (swift build -c release, icon from assets/icon/Manas.icns, generated Info.plist with dev.viraat.manas identity, ad-hoc codesign + strict verify); installed to /Applications/Manas.app and launched — exactly one instance running as a Foreground (Dock) app with the bindu icon resolving through LaunchServices, window renders, launch check-in ran automatically, and state.json survived the transition (2 todos, usage history intact). Added a Launch at login toggle to the settings gear popover backed by SMAppService.mainApp: accent-tinted switch, registers/unregisters on change, surfaces requiresApproval with a System Settings hint, and degrades to disabled-with-caption when running unbundled via swift run. No prior Manas instances were running (the old dev build had already been quit). swift build clean; swift test 136 tests, 0 failures (5 env-gated skips).
- **Interfaces:** scripts/make-app.sh (packaging entry point, outputs dist/Manas.app; dist/ gitignored); bundle id dev.viraat.manas v1.0(1), macOS 14 min, public.app-category.productivity, regular Dock app (no LSUIElement); install location /Applications/Manas.app (rebuild+reinstall: scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/); SettingsPopover in Sources/Manas/UI/Main/MainHeaderView.swift gained the SMAppService.mainApp launch-at-login toggle with isBundled = Bundle.main.bundleURL.pathExtension == "app"; Package.swift untouched (ServiceManagement auto-links); DECISIONS.md bundle rationale section
- **Follow-ups:**
  - Approve login item on first enable — SMAppService register from an ad-hoc-signed app can land in requiresApproval; the toggle shows a System Settings hint but the user has to flip the approval once themselves
  - Notarized DMG / GitHub release for the landing page CTA [out of lane] — site worker left a placeholder download link; needs Developer ID signing which is explicitly out of scope for this task
- **By:** bundle · 2026-07-15T22:08:31.460Z

## bundle: app packaging + install decisions (rationale)
- 2026-07-15 (bundle): `scripts/make-app.sh` is the one packaging entry point: `swift build -c release`, assemble `dist/Manas.app` (Contents/MacOS/Manas, Resources/Manas.icns from assets/icon/Manas.icns, generated Info.plist, PkgInfo), then ad-hoc `codesign --force --deep -s -` and `codesign --verify --strict`. Rebuild + reinstall = `scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/`. `dist/` is gitignored. Notarization is deliberately out of scope (no public release); ad-hoc signing is enough for a locally built app.
- 2026-07-15 (bundle): bundle identity is `dev.viraat.manas`, version 1.0 (1), LSMinimumSystemVersion 14.0, category public.app-category.productivity, NSPrincipalClass NSApplication. It is a regular Dock app — LSUIElement is intentionally NOT set (verified running as ApplicationType Foreground). State stays at `~/Library/Application Support/Manas/state.json` (path is app-support-name based, unaffected by bundle id; verified surviving the swift-run → installed-app transition with todos + usage history intact).
- 2026-07-15 (bundle): launch-at-login lives in `SettingsPopover` (Sources/Manas/UI/Main/MainHeaderView.swift) as a small switch Toggle tinted `.manasAccent`, backed by `SMAppService.mainApp` (ServiceManagement, auto-linked — Package.swift unchanged, zero new dependencies). Bundled detection is `Bundle.main.bundleURL.pathExtension == "app"`: under `swift run` the toggle is disabled with the caption "Available when Manas runs as an installed app."; in the bundle it registers/unregisters on change, reflects `.requiresApproval` with a System Settings hint, and reverts the toggle to the real `SMAppService` status if registration throws. Defaults off (nothing pre-registers it).
- 2026-07-15 (bundle): silent-verification trick for auto-hidden Docks: the Dock bar has no on-screen window to `screencapture -x -l`, so the Dock-icon check is `NSWorkspace.shared.icon(forFile: "/Applications/Manas.app")` rendered to PNG — the same LaunchServices lookup the Dock draws from. Window capture used CGWindowListCopyWindowInfo to find the Manas window id, then `screencapture -x -l <id>` (no focus steal, no shutter sound).
## bundle: Resolved the 3-sided jj merge conflict in DECISIONS.md (the
- **Did:** Resolved the 3-sided jj merge conflict in DECISIONS.md (the only conflicted file). The materialized conflict was a snapshot of main (whose log tail ends with the bundle completion report) plus two diffs: one empty no-op diff and one worker diff appending the bundle packaging rationale section. Kept main content in place and appended the worker section at the end, dropping nothing from either side — same additive-log pattern as the n0/n3/n1/n2/n5 resolutions. Verified: jj resolve --list empty, swift build clean, swift test 136 tests / 0 failures / 5 skipped (env-gated).
- **Interfaces:** DECISIONS.md (bundle completion report retained, bundle app-packaging rationale section appended at end)
- **By:** bundle · 2026-07-15T22:11:48.873Z

## followup: Approve-login-item-on-first-enable implemented: SMAppService first-enable appro…
- **Did:** Approve-login-item-on-first-enable implemented: SMAppService first-enable approval is now a handled handshake — register() throwing while status parks on .requiresApproval keeps the toggle on (intent), shows the approval caption, and opens System Settings › Login items via SMAppService.openSystemSettingsLoginItems(); the popover re-reads status on appear and on NSApplication.didBecomeActiveNotification so the caption clears the moment the user approves and returns. Logic extracted from SettingsPopover into a tested LoginItemController behind a LoginItemManaging seam. Caption text now wraps instead of truncating at the 248pt popover width. Verified: swift build clean; swift test 149 tests / 0 failures (5 env-gated skips) incl. 13 new LoginItemController tests covering approval-with-throw, approval-without-throw, return-approved, return-unapproved, fresh-open-pending, re-enable-reopens-settings, plain enable/disable, failure reverts, unbundled; all three popover states visually verified via silent offscreen NSHostingView renders.
- **Interfaces:** Sources/Manas/UI/Main/LoginItemController.swift: LoginItemManaging protocol (@MainActor: status/register/unregister/openApprovalSettings), MainAppLoginItem (real SMAppService.mainApp impl), LoginItemController (@MainActor @Observable: isAvailable/isEnabled/caption, refresh(), setEnabled(_:), static standard(), static approvalCaption/unbundledCaption/failureCaption); SettingsPopover(loginItem: LoginItemController = .standard()) injectable init in MainHeaderView.swift; Tests/ManasTests/LoginItemControllerTests.swift. NOTE: my workspace predates the bundle node merge — MainHeaderView.swift is byte-based on the bundle node landed version with only the SettingsPopover section rewritten; take this side on conflict.
- **Follow-ups:**
  - Live-verify the approval flow once from the installed /Applications/Manas.app [out of lane] — The requiresApproval state cannot be forced deterministically in automation; one manual first-enable from the installed bundle would confirm macOS actually lands in the handled path end-to-end.
  - Unbundled caption truncation existed in the bundle node version — Fixed here via fixedSize on the caption; if the bundle node lands a different SettingsPopover variant later, keep the wrap fix.
- **By:** followup · 2026-07-15T22:20:02.603Z


## followup: login-item approval decisions (rationale)
- 2026-07-15 (followup, login approval): first-enable approval is now a handled handshake, not an error. On macOS, `SMAppService.register()` can THROW "Operation not permitted" while the status parks on `.requiresApproval` — the bundle node's SettingsPopover treated any throw as failure (toggle reverted, "Couldn't update the login item"). The flow now recognizes that case: the toggle keeps the user's intent (on), the caption says approval is pending, and the app opens System Settings › Login items via `SMAppService.openSystemSettingsLoginItems()` so approving is one click.
- 2026-07-15 (followup, login approval): the state machine lives in `LoginItemController` (`Sources/Manas/UI/Main/LoginItemController.swift`, `@MainActor @Observable`) behind a `LoginItemManaging` seam (`status/register/unregister/openApprovalSettings`; real impl `MainAppLoginItem` wraps `SMAppService.mainApp`) — unit-tested in `Tests/ManasTests/LoginItemControllerTests.swift` (13 tests). `SettingsPopover` gained an injectable defaulted init `SettingsPopover(loginItem: LoginItemController = .standard())`; `.standard()` is disabled (nil item) when not running from a real .app bundle. Only an explicit enable ever opens System Settings — `refresh()` never does.
- 2026-07-15 (followup, login approval): the outcome is picked up automatically: the popover calls `refresh()` on appear and on `NSApplication.didBecomeActiveNotification` (the user returns from System Settings), clearing the caption once the status reads `.enabled`. A pending item found on a fresh popover open (no intent this session) shows toggle OFF + approval caption; a pending enable from this session shows toggle ON + caption. Disabling while pending unregisters the request and clears the intent. Caption text got `.fixedSize(horizontal: false, vertical: true)` — the unbundled caption truncated at width 248 (verified by offscreen NSHostingView renders, n4's pattern).
- 2026-07-15 (followup, login approval): this workspace predates the bundle node's merge, so `MainHeaderView.swift` here is based byte-for-byte on the bundle node's landed version with only the SettingsPopover login-item section rewritten (imports: `ServiceManagement` moved to LoginItemController; `AppKit`+`Combine` added for the activation notification, mirroring ManasApp.swift). Merge resolution should take this side for that file.
## followup: Resolved the 2-file jj merge conflicts for the approve-login-item-on-first-enab…
- **Did:** Resolved the 2-file jj merge conflicts for the approve-login-item-on-first-enable followup. MainHeaderView.swift (2-sided, 5 conflicts): took the worker side wholesale — diffing both parents confirmed the worker version is byte-identical to the bundle node's landed file except for the SettingsPopover login-item section, which is this task's deliverable (LoginItemController-driven toggle replacing main's inline SMAppService code that treated requiresApproval as failure; imports ServiceManagement->AppKit+Combine; refresh on appear + didBecomeActive; wrapping caption). DECISIONS.md (3-sided): materialized as main's snapshot + one no-op diff + a worker diff appending the login-item approval rationale section; kept main's content in place and appended the worker section at the end (extracted programmatically from the diff, nothing retyped), matching the established additive-log pattern. Verified: jj resolve --list empty, no stray markers, swift build clean, swift test 149 tests / 0 failures / 5 skipped (env-gated).
- **Interfaces:** Sources/Manas/UI/Main/MainHeaderView.swift (SettingsPopover(loginItem: LoginItemController = .standard()) injectable init, toggle bound to loginItem.isEnabled/setEnabled, refresh on appear + NSApplication.didBecomeActiveNotification); DECISIONS.md (followup login-item approval rationale section appended at end)
- **Follow-ups:**
  - Live-verify the approval flow once from installed /Applications/Manas.app [out of lane] — requiresApproval cannot be forced deterministically in automation; the worker flagged one manual first-enable from the installed bundle to confirm the handled path end-to-end (rebuild+reinstall via scripts/make-app.sh first so the installed app picks up LoginItemController)
- **By:** followup · 2026-07-15T22:23:07.853Z

## followup-2: Fixed the unbundled login-item caption truncation in the settings
- **Did:** Fixed the unbundled login-item caption truncation in the settings popover. Root cause was popover sizing, not text wrapping: popovers size from the content's first measurement, and the caption only arrived via refresh() in onAppear, so the popover opened at 248x115 (no caption) and the late caption was squeezed into one ellipsized line. Mid-task the sibling followup node landed LoginItemController on main; adapted to that structure instead of the old SMAppService-inline popover. Fix: LoginItemController.init now calls refresh() so isEnabled/caption are correct before the first body evaluation; the popover now measures 248x147 with the caption fully wrapped to two lines (verified by offscreen render PNG). Regression test fails against main-as-landed with the exact 115-vs-147 signature and passes with the fix; all 152 tests pass (5 env-gated skips), sibling's LoginItemControllerTests unmodified.
- **Interfaces:** LoginItemController.init(item:) now refreshes synchronously (byte-identical to main otherwise; MainHeaderView.swift and LoginItemControllerTests.swift synced from main unchanged). New Tests/ManasTests/UI/SettingsPopoverLayoutTests.swift: testFirstMeasurementAlreadyIncludesTheCaption (popover first fittingSize == settled size), testUnbundledCaptionWrapsInsteadOfTruncating (settled height > lineLimit(1) baseline), env-gated MANAS_POPOVER_DUMP=<dir> PNG dump. DECISIONS.md contract: popover-size-affecting state must be computed synchronously before first body evaluation, never only in onAppear/task.
- **Follow-ups:**
  - Installed app at /Applications/Manas.app predates this fix [out of lane] — The bundle node installed the app before this fix and the login-item rework landed; rebuilding and reinstalling (scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/) picks both up.
- **By:** followup-2 · 2026-07-15T22:33:06.119Z

## followup-2: unbundled caption truncation (root cause + contract)
- 2026-07-15 (followup-2, caption truncation): the truncation was NOT a line-limit problem — popovers size themselves from the content's FIRST measurement, and the login-item caption only appeared in `onAppear`/`refresh()`, so the popover opened 32pt too short (measured 248×115 without the caption vs 248×147 settled) and the late caption was squeezed into one ellipsized line. `.fixedSize(horizontal: false, vertical: true)` on the caption Text (which the followup node had already added) only changes how the squeeze fails; it does not size the popover correctly.
- 2026-07-15 (followup-2): fix is in `LoginItemController.init` — it now calls `refresh()` so `isEnabled`/`caption` are correct from birth and the popover's first measurement already includes the wrapped caption. `refresh()` is synchronous and idempotent; the existing `.onAppear`/`didBecomeActive` refreshes are unchanged, and all pre-existing LoginItemControllerTests pass unmodified (tests that skipped the explicit refresh still hold because `setEnabled` guards on `wanted != isEnabled`).
- 2026-07-15 (followup-2): CONTRACT for this popover (and any future popover content): state that affects content size must be computed synchronously before the first body evaluation — never only in `onAppear`/`task`. Guarded by `Tests/ManasTests/UI/SettingsPopoverLayoutTests.swift`: `testFirstMeasurementAlreadyIncludesTheCaption` (first `fittingSize` == settled size; fails with the exact 115-vs-147 signature if the caption goes late again) and `testUnbundledCaptionWrapsInsteadOfTruncating` (settled height beats a `.lineLimit(1)` baseline). `MANAS_POPOVER_DUMP=<dir>` dumps a PNG of the popover for visual checks.
- 2026-07-15 (followup-2): my workspace predates the followup merge, so `LoginItemController.swift` and `LoginItemControllerTests.swift` appear as adds and `MainHeaderView.swift` as a rework here — all three are byte-identical to what the followup node landed on main except the one `refresh()` call (plus comment) in `LoginItemController.init`.
## followup-2: Resolved the 2-file jj merge conflicts for the followup-2
- **Did:** Resolved the 2-file jj merge conflicts for the followup-2 caption-truncation fix. LoginItemController.swift (2-sided): took the worker side wholesale — extracting both sides from the materialized markers confirmed they are byte-identical except the worker's five-line addition in init (a comment plus refresh()), which IS the fix: popovers size from the content's first measurement, so isEnabled/caption must be set before the first body evaluation. DECISIONS.md (2-sided): union following the established additive-log pattern — kept main's content in place (ending with the followup-2 completion report) and appended the worker's followup-2 rationale section (root cause + popover-sizing contract); the shared prefix of both sides was asserted identical programmatically, nothing retyped. Verified: jj resolve --list empty, no stray markers, swift build clean, swift test 152 tests / 0 failures / 6 skipped (env-gated).
- **Interfaces:** Sources/Manas/UI/Main/LoginItemController.swift (init(item:) now calls refresh() synchronously; otherwise byte-identical to main); DECISIONS.md (followup-2 completion report retained, followup-2 caption-truncation rationale/contract section appended); Tests/ManasTests/UI/SettingsPopoverLayoutTests.swift already staged by the worker, untouched
- **Follow-ups:**
  - Rebuild and reinstall /Applications/Manas.app [out of lane] — The installed app predates both the login-item rework and this caption fix; scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/ picks them up
- **By:** followup-2 · 2026-07-15T22:36:11.643Z

## Plan approved
- **What:** Approved a 2-task plan for: what ist he status of manas is it working can you launch it?. Tasks: Day-scoped todos: model + store + judge scoping; Timeline UI: Earlier, Today, Upcoming, Plan a day.
- **Why:** user-approved plan; workers implement these nodes in isolated workspaces, honoring the dependency edges
- **By:** conductor · 1784160102836

## n0: Todos are now day-scoped. Todo gained day: Date (start-of-day,
- **Did:** Todos are now day-scoped. Todo gained day: Date (start-of-day, defaults to createdAt day; decodeIfPresent migration backfills old state.json todos — the real ~/Library/Application Support/Manas/state.json was verified to be old-format and its shape is covered by tests). AppStore exposes todos(on:), todosToday, pastDays (newest first) and upcomingDays (soonest first); addTodo(_:on: Date = Date()) inserts at the top of its day group (old call sites compile via the default); moveToToday re-dates an unfinished past todo to today and clears its verdict; addDiscoveredToTodos still lands on today. Judge scoping: judgeToday sends only todosToday, the empty-day CLI skip checks only today, and applyJudgeResult guards verdicts with isDateInToday so past days are frozen and future todos are immune. swift build clean; swift test 161 tests (9 new), 0 failures, 6 env-gated skips. No UI files touched.
- **Interfaces:** Todo.day: Date + init(day: Date? = nil) + custom init(from:) migration, normalizing every decoded day to start-of-day; DayGroup { day, todos } (Identifiable by day, mirrors CheckInDay — pastDays/upcomingDays return it, so ForEach binds directly) (Sources/Manas/Models/Todo.swift); AppStore.todos(on:)/todosToday/pastDays/upcomingDays/addTodo(_:on:)/moveToToday(_:) + day-guarded applyJudgeResult (Sources/Manas/Store/AppStore.swift); judgeToday scopes to todosToday incl. empty-day skip (Sources/Manas/Store/AppStore+Judge.swift); new tests in AppStoreTests, JudgeTodayFlowTests, ModelCodingTests; DECISIONS.md n0 day-scoping contract section
- **Follow-ups:**
  - UI for past (read-only) and upcoming (planning) days [out of lane] — The store now exposes pastDays/upcomingDays and moveToToday, but Sources/Manas/UI was out of this lane — the header date navigation is still local state showing today only
  - Rebuild and reinstall /Applications/Manas.app [out of lane] — The installed bundle predates day scoping; scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/ picks it up (its old-format state.json migrates on first launch)
- **By:** n0 · 2026-07-16T00:08:59.712Z

## n0: day-scoped todos (model + store + judge scoping) — interface contract
- 2026-07-15 (n0 day-scoping): `Todo` gained `var day: Date` — the calendar day (start of day, `Calendar.current`) the todo belongs to. The initializer takes `day: Date? = nil` right after `createdAt` and normalizes ANY passed date to its start of day; nil defaults to `startOfDay(for: createdAt)`. Decoding migration: `day` decodes via `decodeIfPresent` falling back to `startOfDay(for: createdAt)`, so pre-day state.json files (the real one at ~/Library/Application Support/Manas/state.json has no `day` keys — verified) load losslessly with todos backfilled to their created-at day. Encoding is synthesized, so every save now writes `day`.
- 2026-07-15 (n0 day-scoping): `AppStore` day-grouped accessors: `todos(on: Date) -> [Todo]` (same calendar day, list order), `todosToday`, `pastDays` / `upcomingDays` (both `[(day: Date, todos: [Todo])]`; days before/after today that have todos; past newest first, upcoming soonest first; today in neither). `todos` stays the single persisted flat array — UI should render groups via these accessors, and cross-day array order is meaningless.
- 2026-07-15 (n0 day-scoping): `addTodo(_:on:)` takes `on day: Date = Date()` (old one-arg call sites compile unchanged) and inserts at the top of that day's group via a private `insert(_:)` helper; `addDiscoveredToTodos` routes through the same helper and keeps landing on today. `moveToToday(_ id:)` re-dates an UNFINISHED PAST todo to today (top of today's group) and clears its verdict for a fresh judgment; done, today, and future todos are ignored.
- 2026-07-15 (n0 day-scoping): judge scoping — `judgeToday` sends `todosToday` (never the whole array) to the judge, and its empty-day skip now checks `todosToday.isEmpty` (a store holding only past/future todos with no observed activity skips the CLI). `applyJudgeResult` guards every verdict with `calendar.isDateInToday(todo.day)`: past days are frozen (their verdicts never change, even on a direct id match) and future todos can never receive one. Discovery dedupe still checks ALL todo texts across days.
- 2026-07-15 (n0 day-scoping): tests — 9 new (161 total, 0 failures, 6 env-gated skips): old-JSON migration at both Todo and full-state level, day normalization, group accessors/ordering, moveToToday accept+ignore paths, applyJudgeResult past/future immunity, prompt exclusion (JudgePromptBuilder output provably free of past/future ids/texts via the flow test), and the past/future-only empty-day skip.

## n0: Resolved the 2-sided jj merge conflict in DECISIONS.md (the
- **Did:** Resolved the 2-sided jj merge conflict in DECISIONS.md (the only conflicted file) for the day-scoped-todos node. Main side held the Plan approved entry and the n0 completion report; the worker side was a pure-addition diff appending the n0 day-scoping interface-contract rationale section. Kept main content in place and appended the worker section after it (extracted programmatically from the materialized markers, nothing retyped), matching the additive-log pattern of all prior resolutions. Verified: jj resolve --list empty, no stray markers, swift build clean, swift test 162 tests / 0 failures / 6 env-gated skips.
- **Interfaces:** DECISIONS.md (n0 completion report retained, n0 day-scoped-todos interface-contract section appended); no Swift files needed conflict resolution — Todo.swift/AppStore.swift/AppStore+Judge.swift and tests auto-merged
- **Follow-ups:**
  - Rebuild and reinstall /Applications/Manas.app [out of lane] — The installed bundle predates day scoping; scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/ picks it up
- **By:** n0 · 2026-07-16T00:43:15.388Z

## n1: Screen 1 is now a scrolling day timeline, visually
- **Did:** Screen 1 is now a scrolling day timeline, visually verified live. Inside the existing ScrollView, top to bottom: (1) EarlierSection — shown only when pastDays is non-empty, a muted disclosure row (Earlier · 3 days, chevron rotates) collapsed on every launch, expanding to one read-only card per past day newest first (Yesterday / Monday, Jul 13 headers; checkbox rendered as state not a control; verdict chip + evidence frozen with no accept/dismiss; unfinished todos get a ghost Move to today button calling store.moveToToday). (2) Today unchanged — header, pinned AddTodoField, TodoListSection now bound to todosToday, DiscoveredSection. (3) UpcomingSection — one muted section per future day (Tomorrow / Friday, Jul 17 header, surface-1 card of plain rows where the checkbox toggles isDone, no verdict UI) each ending in a compact per-day add field calling addTodo(_:on:). (4) A + Plan a day… ghost button opening a popover with a graphical DatePicker restricted to tomorrow onward; picking a date (click = immediate, button confirms the preselected default) reveals a session-local empty section for that day and focuses its add field after 250ms. Day names come from a shared DayLabel helper (Today/Tomorrow/Yesterday, else weekday + date). Everything stays inside ContentView.contentMaxWidth. Verified: swift build clean; swift test 171 tests / 0 failures / 8 env-gated skips (9 new tests: DayLabel relative-name + format logic, Earlier hidden/collapsed/expanded layout, Upcoming sections layout, plus env-gated PNG dump and state-seed diagnostics). Visual checks were fully silent: offscreen NSHostingView PNG dumps (full timeline, expanded Earlier, plan popover, freshly planned empty day), then the real app launched twice against a seeded scratch state with auto-check-ins disabled and captured via screencapture -x -l <windowID> — the user's running Manas instance, real state.json, and window-frame default were left untouched (frame temporarily enlarged for capture, restored after). README feature list now describes the day timeline.
- **Interfaces:** Sources/Manas/UI/Main/DayLabel.swift: DayLabel.title(for:relativeTo:calendar:); Sources/Manas/UI/Main/EarlierSection.swift: EarlierSection(initiallyExpanded: Bool = false) (private PastDayCard/PastTodoRow); Sources/Manas/UI/Main/UpcomingSection.swift: UpcomingSection(initiallyPlanned: Set<Date> = []) (session-local plannedEmptyDays + FocusState per day; private UpcomingDaySection/UpcomingTodoRow/UpcomingAddField), PlanDayPicker(onPick:); ContentView scroll order Earlier→Today→Discovered→Upcoming; TodoListSection reads store.todosToday; AppStore.previewTimeline + samplePastTodos/sampleUpcomingTodos in PreviewData.swift; dev seams: MANAS_STATE_FILE (AppStore.defaultStateURL override), MANAS_DISABLE_AUTO_CHECKS (startAutoCheckIns no-op), MANAS_TIMELINE_DUMP / MANAS_SEED_STATE (TimelineLayoutTests diagnostics); DECISIONS.md n1 timeline rationale section appended
- **Follow-ups:**
  - Live-verify plan-a-day focus handoff interactively — The popover→new-section→focused-add-field flow is implemented with a 250ms focus delay and verified by code + renders, but a real click-through could not be done silently (clicking would steal focus from the user); one interactive try would confirm the field ends up focused on real hardware.
  - Rebuild and reinstall /Applications/Manas.app [out of lane] — The installed bundle predates day scoping and the timeline UI; scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/ picks both up.
  - Header date navigation could drive the timeline [out of lane] — The header chevrons still only change local selectedDate; with the timeline in place they could scroll to / highlight the corresponding day section instead.
- **By:** n1 · 2026-07-16T01:00:20.241Z

## n1: day timeline UI decisions (rationale)
- 2026-07-15 (n1 timeline): Screen 1's scroll is now the day timeline: `EarlierSection()` (collapsed disclosure over read-only past-day cards), `TodoListSection` (now bound to `store.todosToday`), `DiscoveredSection`, `UpcomingSection()` (one muted section per future day + the plan-a-day button), in that order inside ContentView's ScrollView. The header and pinned `AddTodoField` are unchanged and stay outside the scroll; `AddTodoField` still adds to today.
- 2026-07-15 (n1 timeline): day names come from `DayLabel.title(for:relativeTo:calendar:)` (Sources/Manas/UI/Main/DayLabel.swift, tested) — Today/Tomorrow/Yesterday, otherwise weekday + date ("Monday, Jul 13"). Use it for any future day-labeled UI.
- 2026-07-15 (n1 timeline): past rows render frozen — checkbox is an Image (state, not a control), verdict chip/evidence shown without accept/dismiss; the only live control is a ghost "Move to today" on unfinished todos (`store.moveToToday`). Earlier defaults to collapsed on every launch (deliberately not persisted); `EarlierSection(initiallyExpanded:)` exists for previews/tests.
- 2026-07-15 (n1 timeline): upcoming rows are plain (checkbox toggles isDone, no verdict UI — the judge never sees the future); each future day's card ends in a compact add field calling `store.addTodo(_:on:)` for that day. A day picked in the plan-a-day popover that has no todos yet lives in `UpcomingSection`'s session-local `@State plannedEmptyDays` (visible with a focused add field, not persisted while empty — first todo moves it into the store). `PlanDayPicker` restricts its graphical calendar to tomorrow onward; clicking a date plans it immediately, a "Plan this day" ghost button confirms the pre-selected default (re-picking the selected date fires no change event).
- 2026-07-15 (n1 timeline): two dev/verification env seams: `MANAS_STATE_FILE=<path>` points `AppStore.defaultStateURL` at a scratch state file, and `MANAS_DISABLE_AUTO_CHECKS=1` makes `startAutoCheckIns` a no-op — together they let a launched app be screenshot-verified with seeded data without touching the real state.json or spending a CLI check-in. `MANAS_TIMELINE_DUMP=<dir>` (TimelineLayoutTests) dumps offscreen PNGs of the timeline states, following the MANAS_POPOVER_DUMP pattern.
## n1: Resolved the 2-sided jj merge conflict in DECISIONS.md (the
- **Did:** Resolved the 2-sided jj merge conflict in DECISIONS.md (the only conflicted file) for the n1 timeline-UI node; all Swift/UI/test files auto-merged. The materialized conflict was mains snapshot (ending with the n1 completion report) plus a worker diff that was a pure addition appending the n1 day-timeline-UI rationale section. Kept main content in place and appended the worker section after it — both sides extracted programmatically from the markers, nothing retyped, matching the additive-log pattern of every prior resolution (n0/n3/n1/n2/n5/bundle/followup). Verified: jj resolve --list empty, no stray markers, swift build clean, swift test 171 tests / 0 failures / 8 env-gated skips.
- **Interfaces:** DECISIONS.md (n1 completion report retained, n1 day timeline UI decisions rationale section appended at end); no code files needed resolution — DayLabel.swift, EarlierSection.swift, UpcomingSection.swift, ContentView.swift, TodoList.swift, PreviewData.swift, AppStore(+Judge).swift, README.md and new tests auto-merged
- **Follow-ups:**
  - Timeline redesign: replace Plan-a-day picker and collapsed Earlier with a continuous scrollable day feed [out of lane] — RUDDER.md plan was REVISED after n1 landed — the user rejected the Plan-a-day date-picker and the Earlier disclosure in favor of scrolling up into past days / down into future days with today anchored and a floating Today pill; this merge lands the superseded design as the working baseline for that rework
  - Rebuild and reinstall /Applications/Manas.app [out of lane] — The installed bundle predates day scoping and the timeline UI; scripts/make-app.sh && rm -rf /Applications/Manas.app && cp -R dist/Manas.app /Applications/ picks both up
- **By:** n1 · 2026-07-16T02:40:25.077Z

## worker: Shipped Manas 0.3.5. Fixed the scroll jump on expand/collapse:
- **Did:** Shipped Manas 0.3.5. Fixed the scroll jump on expand/collapse: SectionKey.group was keyed by group label with no day, so folding today's Work folded Work on every day in the feed at once, including the history above the viewport - the content above shrank, the ScrollView kept its raw offset, and the page jumped (measured -141pt under the cursor). Keys now carry the day. Also fixed: the app could open days back in history because one proxy.scrollTo across an unrealized LazyVStack lands short (launch and jump-to-today now measure and correct); the floating Today pill bound its own Cmd-T while the menu and header said Cmd-L and skipped the compose hand-off; both fold animations ignored Reduce Motion. Added a MANAS_DISABLE_SYNC seam after a scratch build read the installed app's keychain and pushed 50 seed todos to the live account - tombstoned within ~2 minutes, verified 135 real rows live and 0 test rows, and the local app now holds exactly those 135. Released end to end: notarized, stapled, GitHub release v0.3.5, appcast signed and deployed to manas.viraat.dev, installed to /Applications, launched, and the fix re-verified on the shipped build against real data at +0.0pt with all six past-day headers unmoved.
- **Interfaces:** SectionKey.group(_ label: String, on day: Date) -> String, was group(_:); fold keys now carry yyyy-MM-dd. New SectionKey.dayKey(_:), dayComponent(of:), isStaleGroupKey(_:). AppStore.pruningCollapsedSections(_:toDaysWithTodos:) drops per-day keys for days with no todos plus dayless legacy keys, on load and on save. SyncController.isDisabledByEnvironment reads MANAS_DISABLE_SYNC. New SignedOutSyncAuth, a no-op SyncAuth that never reaches keychain or network. TodoGroupBlock takes a day: Date.
- **Delivery:** deployed; release; target=Manas 0.3.5 - GitHub release, Sparkle appcast on manas.viraat.dev, and /Applications/Manas.app; revision=f2911a1; verifiedAt=2026-08-04T12:15:00-07:00; checks=11
- **By:** worker · 2026-08-04T19:14:26.572Z

## worker: Shipped Manas 0.3.5. Fixed the scroll jump on expand/collapse:
- **Did:** Shipped Manas 0.3.5. Fixed the scroll jump on expand/collapse: SectionKey.group was keyed by group label with no day, so folding today's Work folded Work on every day in the feed at once, including the history above the viewport - the content above shrank, the ScrollView kept its raw offset, and the page jumped (measured -141pt under the cursor). Keys now carry the day. Also fixed: the app could open days back in history because one proxy.scrollTo across an unrealized LazyVStack lands short (launch and jump-to-today now measure and correct); the floating Today pill bound its own Cmd-T while the menu and header said Cmd-L and skipped the compose hand-off; both fold animations ignored Reduce Motion. Added a MANAS_DISABLE_SYNC seam after a scratch build read the installed app's keychain and pushed 50 seed todos to the live account - tombstoned within ~2 minutes, verified 135 real rows live and 0 test rows, and the local app now holds exactly those 135. Released end to end: notarized, stapled, GitHub release v0.3.5, appcast signed and deployed to manas.viraat.dev, installed to /Applications, launched, and the fix re-verified on the shipped build against real data at +0.0pt with all six past-day headers unmoved.
- **Interfaces:** SectionKey.group(_ label: String, on day: Date) -> String, was group(_:); fold keys now carry yyyy-MM-dd. New SectionKey.dayKey(_:), dayComponent(of:), isStaleGroupKey(_:). AppStore.pruningCollapsedSections(_:toDaysWithTodos:) drops per-day keys for days with no todos plus dayless legacy keys, on load and on save. SyncController.isDisabledByEnvironment reads MANAS_DISABLE_SYNC. New SignedOutSyncAuth, a no-op SyncAuth that never reaches keychain or network. TodoGroupBlock takes a day: Date.
- **Delivery:** deployed; release; target=Manas 0.3.5 - GitHub release, Sparkle appcast on manas.viraat.dev, and /Applications/Manas.app; revision=f2911a1; verifiedAt=2026-08-04T12:15:00-07:00; checks=11
- **By:** worker · 2026-08-04T19:16:20.697Z

## worker: Shared groups over phone numbers. You share any todo
- **Did:** Shared groups over phone numbers. You share any todo group with a number; that person sees the same bucket in their Manas, can add to it and tick things off, and every line in a shared group carries a small circular avatar on its right for whoever wrote it (initials from a name they set, else the last two digits; color is a stable FNV-1a slot in a muted 6-color identity palette). The group header wears the people it is shared with and opens the share popover (invite by number + optional name, member list with remove, stop sharing / leave). iOS gets the same: avatars on rows and headers, a share sheet, and shared buckets in the compose bar and move-to menus. Grouping now keys on TodoDestination (label + optional shareID) rather than the label, so a shared Manas and a private Manas are two buckets that can never pour into each other; the judge only ever sets the label, never the share. Backend: supabase/migrations/20260806120000_shared_groups.sql adds shared_groups + shared_group_members and todos.share_id/author_id, with SECURITY DEFINER membership helpers (the members policy would otherwise recurse) and a trigger that stamps author_id. Verified the RLS end to end on a scratch Postgres (docker + a stub auth schema): it caught a hole where an outsider holding a share id could insert todos into it, since user_id defaults to the caller — writes are now (share_id is null and user_id = me) or is_share_member(share_id). Also hardened the client against wedging sync: SyncMerge never pushes a row it lacks rights to and never renumbers someone else re-orderable rows, and ShareMerge drops tombstones the server no longer shows. Fixed a pre-existing iOS compile break (MobileDayFeedView still called the one-arg SectionKey.group). swift build clean, 293 tests / 0 failures (38 new), iOS xcodebuild BUILD SUCCEEDED, UI checked via offscreen PNG dumps.
- **Interfaces:** New: Models/SharedGroup.swift (PhoneIdentity, SharedGroup, SharedGroupMember, MemberBadge, TodoDestination); Store/AppStore+Sharing.swift (sharedGroups, shareGroup(_:withPhone:memberName:now:), addMember, removeMember, stopSharing, leaveSharedGroup, setMyDisplayName, applyShareMerge, author(of:), memberLabel, isAuthoredByCurrentUser); Sync/SharedGroupRecord.swift (SharedGroupRecord, SharedGroupMemberRecord, ShareMerge); Sync/SupabaseShareAPI.swift; Sync/PostgRESTClient.swift (transport extracted from SupabaseTodoAPI, which lost its baseURL/anonKey fields); Design/MemberAvatar.swift (MemberAvatar, MemberAvatarStack, Color.memberPalette/memberTint); UI/Main/ShareGroupPopover.swift; ios/App/MobileShareGroupSheet.swift (SharedGroupTarget). Changed: Todo gains shareID/authorPhone and .destination; TodoGroup gains shareID and its id is now TodoDestination.key; SectionKey.group(_:on:) gains a TodoDestination overload (string form for private groups is unchanged); AppStore gains currentPhone, sharedGroupRecords, sharedMemberRecords, myDisplayName, availableDestinations, standingDestinations, resolve(_:), emoji(for:), addTodo(_:on:destination:), setTodoGroup(_:to:); todoGroups(on:) clusters by destination; SyncMerge.merge gains currentPhone + liveShareIDs; TodoRecord gains share_id/author_id; TodoDragController works in TodoDestination not string keys; TodoGroupPickerButton binds a TodoDestination; ios/project.yml compiles AppStore+Sharing.swift.
- **Follow-ups:**
  - Apply the migration to the live Supabase project and redeploy both apps [out of lane] — supabase/migrations/20260806120000_shared_groups.sql is verified only against a scratch Postgres. Until it is pushed to gdnknuiqxmosuwoytrzc, a client that sends share_id/author_id will 400 (PGRST204, unknown column) and sync will fail for everyone — so the migration must land before or with any release carrying this code.
  - Real end-to-end check with two phone numbers [out of lane] — The two test OTP numbers in supabase/BACKEND.md (+1 555 555 0100 and +1 415 555 0137) make a genuine two-account test possible once the migration is live: share from one, add from the other, confirm the avatars and the tick-off both travel.
  - Renaming a shared group is not exposed in the UI [out of lane] — SharedGroupRecord.name syncs and AppStore.setSharedGroupEmoji exists, but nothing lets the owner rename a share; the name is fixed at whatever the group was called when it was first shared.
- **By:** worker · 2026-08-06T18:03:29.660Z

## release gate: close the shared-todo relocation hole and unpin CI from a crashing compiler
- **Did:** Pre-merge gate on the sharing PR turned up three RLS holes and one seven-day-old CI failure. (1) `todos update` evaluates USING against the old row and WITH CHECK against the new one with nothing tying them together, so a member of one shared group could carry another member's row *sideways* into a second group — exposing its text to people the owner never shared with. The migration's comments had reasoned about pulling a row out to private (correctly blocked) but not about a move between shares. Same shape let a member sign someone else's row over to themselves in place, and rewrite `author_id` so the avatar lied about who wrote a line. Fixed with `guard_shared_todo_moves`, a BEFORE UPDATE trigger — the only place both row versions are in scope. It fires only when a phone claim is present (so the migration's own backfill and service-role repair still run) and only when the destination is another share (so `on delete set null` still cascades on revoke). (2) Added the negative half of the boundary to scripts/e2e.sh: `share_checks` drives two real accounts through the live backend and asserts what a member must *not* be able to do, re-reading each row rather than trusting the status code, since a policy denial via USING returns 200 and zero rows changed. All three holes were reproduced against a scratch Postgres with the real migrations applied in order, then confirmed closed there and against production. (3) CI had been red for 21 consecutive runs since 2026-07-29 (`b19e231`, analytics): macos-15 defaults to Xcode 16.4, whose SILGen crashes lowering the lazy global initializer for `UsageAnalytics.shared` — a compiler crash, never a failing test, while `swift build && swift test` stayed green locally on Xcode 26. Moved the workflow to macos-26 and made toolchain drift off Swift 6.x fail the run loudly.
- **Interfaces:** supabase/migrations/20260806120000_shared_groups.sql gains `public.guard_shared_todo_moves()` + the `todos_guard_shared_moves` BEFORE UPDATE trigger — a client that re-files a row it does not own now gets a `check_violation`, and `author_id` is preserved from the stored row on every update rather than taken from the request. scripts/e2e.sh gains `share_checks()`, run by both `backend` and full modes. .github/workflows/e2e.yml runs on macos-26 with a Swift 6.x assertion.
- **Follow-ups:**
  - The owner must be on their own roster [not a defect] — rows enter a share by membership, so `createShare` seeding an owner membership row is load-bearing, not bookkeeping; both test harnesses had to mirror it before anything passed.
  - Unsolicited shares are possible [out of lane] — any account can add any phone number to a group it owns, so a stranger's group can appear in your feed. No data of yours is exposed by it, but there is no invite-accept step and no block list.


## release gate: shipped Manas 0.4.0 and iOS build 17
- **Did:** Published shared groups on both platforms. Mac 0.4.0 (build 22): notarized, stapled, GitHub release v0.4.0, Sparkle appcast regenerated and signed, site deployed — verified from the outside rather than from the build script's own say-so (releases/latest resolves to v0.4.0, the public download's SHA256 matches the local artifact at fa4272c5, `spctl` reports "Notarized Developer ID", `stapler validate` passes, and the live appcast advertises 0.4.0 with an EdDSA signature). iOS build 17 uploaded and confirmed VALID and IN_BETA_TESTING on App Store Connect. The shared_groups migration was applied to the live project first, because the schema has to lead a client that sends share_id.
- **Delivery:** deployed; release; target=Manas 0.4.0 — GitHub release, Sparkle appcast on manas.viraat.dev, TestFlight build 17; revision=07e4db1; verifiedAt=2026-08-06T12:20:00-07:00
- **Follow-ups:**
  - SHIPPED WITHOUT CI [override, user-approved twice] — GitHub Actions created no run for any commit in this release, including the merge to main and three deliberate re-triggers (push synchronize, PR reopen, direct push); the repo is public, Actions enabled, workflow active, YAML valid, so the cause is GitHub-side. What stands behind 0.4.0 is a full local `scripts/e2e.sh` run on Xcode 26.6 — the toolchain the artifacts are built with — plus the RLS boundary verified against live production. Re-run the workflow when Actions recovers; the macos-26 pin has never actually executed.
  - External TestFlight testers are not on build 17 [out of lane] — the external group holds 4 testers and every build since 15 sits at READY_FOR_BETA_SUBMISSION, so external distribution has never been used. Internal testing is live.
  - 0.3.5 Mac clients now see shared rows [out of lane] — the migration widened `todos select`, so a Mac still on 0.3.5 renders rows from a shared group as ordinary todos, without avatars or a separate bucket. It does not wedge sync (omitted columns stay unchanged on upsert, so the move guard never fires), and 0.4.0 auto-update closes it within a day.
- **By:** release gate · 2026-08-06T19:20:00.000Z

## release gate: contacts picker for sharing, and Caps Lock quick capture repaired
- **Did:** (1) Sharing a group started from a typed phone number, which is a number nobody remembers. Both platforms now lead with the system contact picker — `CNContactPickerViewController` on iOS, `CNContactPicker` on macOS — chosen over `CNContactStore` deliberately: the picker is out of process and returns only the tapped property, so it needs no Contacts permission, no `NSContactsUsageDescription`, and adds nothing to the App Store privacy label, which matters with a submission in review. Both are configured to display phone numbers only, which forces a drill-down to a specific number; picking a whole contact would silently take the first of several. The picked contact fills the form rather than sharing outright, so the number stays visible and correctable, and a name already typed is never overwritten. Typing a number by hand remains for people not in contacts. (2) Fixed double-tapping Caps Lock silently dying after any update. `requestAccessibilityPermissionOnce` prompted only when untrusted AND never-prompted, recording the prompt in UserDefaults forever — but macOS binds an Accessibility grant to the code signature, which every Sparkle update replaces. So the first update after granting killed the shortcut permanently: trust gone, prompt never re-fired, and `addGlobalMonitorForEvents` returns a live-looking monitor that is simply deaf without trust, so nothing surfaced. Trust is now tracked across launches and a grant that vanishes re-arms the prompt; granting also takes effect without a relaunch, since a monitor registered while untrusted stays deaf and has to be re-registered on activation.
- **Interfaces:** New ios/App/ContactPicker.swift (`ContactPicker`, a `UIViewControllerRepresentable`) and Sources/Manas/UI/Main/ContactPickerButton.swift (`ContactPickerButton` + `ContactPickerAnchor`, since the AppKit picker is a popover needing a positioning view). MobileShareGroupSheet and ShareGroupPopover gain the picker above the number field. `QuickCaptureShortcutController` gains `rearmPromptIfTrustWasLost(_:trusted:)`, `grantedBeforeKey`, and re-registration on `didBecomeActive`; `permissionPromptKey`/`grantedBeforeKey` are internal so the re-arm is testable.
- **Follow-ups:**
  - The macOS picker's whole-contact callback is unreachable [by design] — AppKit sends `didSelectContact` only when `displayedKeys` is empty, and this sets it, so only the property callback fires. The first draft carried a dead single-number fallback until the SDK header said otherwise.
  - Nothing in the UI reports a missing Accessibility grant [out of lane] — the app re-asks now, but if someone dismisses that prompt there is still no visible explanation for why Caps Lock does nothing. Settings is the natural home for it.
- **By:** release gate · 2026-08-06T20:05:00.000Z

## release gate: a finished todo could come back the next day
- **Did:** Fixed crossed-off todos reappearing, uncrossed, at the top of the next day. `carryForwardOverdueTodos` correctly skips finished todos, and `contentKey` correctly includes `isDone`, so neither was the fault — the fault was *when* it ran. Both platforms rolled the day forward at launch from local state, before sync's first pass: ContentView's `.task` ran it while sync started in a separate task, and RootView ran it immediately before `sync.start`. A device closed since yesterday still believes yesterday's todos are unfinished, so it moved a todo that had been crossed off elsewhere — and since moving it changes `day` and clears the verdict, that device became the content-dirty side and won the three-way merge. The finished todo came back on every device. Carrying forward now awaits the first successful sync, bounded by a timeout so an offline or failing launch still rolls the day over; signed-out and sync-disabled launches skip the wait entirely.
- **Interfaces:** New Sources/Manas/Store/AppStore+CarryForward.swift: `SyncConvergence` (`carriesRemoteState`, `hasSyncedSinceLaunch`, with `SyncController` conforming via `isSignedIn`/`lastSyncedAt`) and `AppStore.carryForwardOverdueTodos(awaiting:timeout:pollInterval:now:) async`. ContentView awaits it; RootView starts sync *before* awaiting it, or the wait would never end. The synchronous `carryForwardOverdueTodos(now:)` is unchanged and still used by the day-change handler, where state is already live. ios/project.yml compiles the new file — the iOS target lists Store sources one by one, so a new one is invisible until added, which the iOS build caught.
- **Follow-ups:**
  - The wait is a poll, not a signal [out of lane] — `SyncController` has no "converged" notification, so this polls `lastSyncedAt` every 100ms up to the timeout. A continuation resumed by the sync loop would be tidier.
- **By:** release gate · 2026-08-06T20:45:00.000Z

## release gate: sharing is one step
- **Did:** The contact picker landed as an autofill for a form — pick a person, then still fill in their name, your name, and press Share. That is four decisions to share a list with someone, and the user reported it as "not working properly", which it effectively wasn't. Picking someone now shares immediately on both platforms: their name rides along from the contact card, and the invitee-name field is gone because the contact card already answers it. "You appear as" moved out of the share surface entirely to the sync settings block, where it is set once — it describes you, not the group you happen to be sharing. Typing a number stays as the fallback for someone not in contacts, and on iOS its Share button only appears once the number is usable, so the panel is one row until it needs to be two.
- **Interfaces:** MobileShareGroupSheet drops `myName` and the invitee-name field; `adopt(number:name:)` now shares rather than filling the form. ShareGroupPopover drops `identityField` and `myName`. SyncSettingsSection gains `@Environment(AppStore.self)` and the "You appear as" field, seeded on appear.
- **By:** release gate · 2026-08-06T21:30:00.000Z

## release gate: the contacts picker was broken twice, for two different reasons
- **Did:** Ran the picker instead of reading it, which is what should have happened before it shipped. Two real defects, neither visible in a diff. (1) iOS presented `CNContactPickerViewController` as the root of a SwiftUI `.sheet`. It is a remote view controller; as a sheet root it renders but never calls its delegate, so taps did nothing. Now presented through UIKit from the top-most view controller. (2) With that fixed the simulator log gave the second bug away: `[CNUI ERROR] Person selection predicate is set but the delegate does not implement contactPicker:didSelectContact:. The predicate will be ignored.` The `predicateForSelectionOfContact = false` meant to force a drill-down to one number was discarded, so iOS selected the whole contact and had nowhere to deliver it. Both delegate methods are now implemented and the predicate is `phoneNumbers.@count == 1` — one number picks in a tap, several drill in. (3) macOS anchored the `CNContactPicker` popover to a 0×0 view with a `.zero` rect, which cannot position an NSPopover, so it never appeared; it now hangs off the button's real bounds behind a window/bounds guard. Verified on the simulator end to end: picker presents, and a driven selection logs `contact picked: +1 415 555 0137 name=Probe Person` with the CNUI error gone.
- **Interfaces:** ios/App/ContactPicker.swift is now `ContactPickerPresenter` (a `@MainActor` singleton, `@preconcurrency CNContactPickerDelegate`) instead of a `UIViewControllerRepresentable`; `present(onPick:)` is called directly from the button. New DEBUG seam `-manasProbeContactPicker` presents the picker and drives a synthetic selection, so the callback path is checkable without a tap. `ContactPickerAnchor.show` guards on `view.window != nil && !view.bounds.isEmpty` and passes `view.bounds`. AGENTS.md gains a `## Deploy Configuration` section with the release sequence and the operational rules behind these failures.
- **Follow-ups:**
  - macOS is fixed by inspection, not by click [out of lane] — the anchor defect is unambiguous and the guard makes the broken state impossible, but driving the mac popover through the accessibility API was abandoned after the scratch-build launch put a Sparkle error on the user's screen. Confirm by opening a group's share popover and clicking Choose from Contacts.
- **By:** release gate · 2026-08-06T21:55:00.000Z

## release gate: one unwritable row was stopping members receiving anything
- **Did:** Root-caused "the person I shared with can't see the group". `syncShares` pushed `groups.toPush` and `members.toPush` with no ownership filter. Row-level security lets only the owner write a `shared_groups` row, so a member's client sent back the row it had just pulled and got HTTP 403 (`42501`, verified against production with a real member token). PostgREST fails the whole batch, the throw aborted `syncShares` — and shares sync *before* todos are fetched, so that member never received a single shared todo, on every launch, silently. The todo push has carried an `isWritable` guard from the start for exactly this reason ("one rejected row fails the whole batch and wedges every later sync"); the share push never got one. Added `ShareMerge.pushable(groups:members:knownGroups:currentPhone:)` and routed the push through it. Evidence trail: the owner's share had 3 members and 0 todos carrying its share_id (repaired in place), a member's token fetched both todos and the group fine over curl, and a fresh simulator sign-in as a member rendered the group correctly — which is why it reproduced only for a device that already held share records.
- **Interfaces:** New `ShareMerge.pushable(...)` (owner writes the group row and anyone's membership; everyone else only their own membership). New `AppStore.setMemberName(_:forMemberWithPhone:in:now:)` with the same permission rule. Both share surfaces gained naming — `MobileShareGroupSheet` via a tap-to-name alert, `ShareGroupPopover` via edit-in-place next to the avatar — so a member without a name stops rendering as the last two digits of their number.
- **Follow-ups:**
  - A wedged sync is invisible [out of lane] — `SyncController.phase` goes to `.error`, but neither platform surfaces it where a member would look, so this failure was indistinguishable from an empty group. That, more than the missing guard, is why it took a database session to find.
  - Auto-detecting names for existing members would need Contacts read access [decided against] — `CNContactStore` means a permission prompt and a privacy-label change. The picker deliberately avoids both, so names come from the invite or from naming someone by hand.
- **By:** release gate · 2026-08-06T22:40:00.000Z

## Plan approved
- **What:** Approved a 3-task plan for: on the app it still shows the last two number - make it hte initials of the better and fix the sharing thing it's not working on krithiks number. Tasks: Fix cross-device share membership; Contacts-backed member initials; Integration pass and release readiness.
- **Why:** user-approved plan; workers implement these nodes in isolated workspaces, honoring the dependency edges
- **By:** conductor · 1786082535031

## n0: Root-caused and fixed the share that never reached (309)
- **Did:** Root-caused and fixed the share that never reached (309) 826-4765. Live evidence first: shared_group_members held him as 3098264765 while auth.users authenticates him as 13098264765, so is_share_member compared 10 digits to 11 and RLS correctly hid the group and both its todos from him — invisible from the owner side, which is why it looked like an empty group. PhoneIdentity.normalized returned bare digits, so a number typed the way people say it was stored 10-long while one picked from a contact card carrying +1 was stored 11-long: one roster, two identity schemes. Fixed at every layer. Client: PhoneIdentity.canonical(_:signedInAs:) resolves a missing country code from the inviter own identity by length (within one country national numbers are the same length, so what the reference carries in front of a number that long IS the calling code), validated against a curated PhoneRegion table and refused rather than guessed when unresolvable; AppStore.canonicalPhone routes every human-entry point (invite field, contacts picker, both share surfaces) and is nil until currentPhone arrives. macOS sign-in was doing "+" + digits, sending the OTP provider +3098264765 for a bare US number; it now fills in the device region code via PhoneIdentity.e164. Server: migration 20260807060000 adds canonical_phone_id + a BEFORE INSERT/UPDATE trigger (so the shipped 0.4.5 clients stop writing broken rows before everyone updates) plus a one-time repair keyed on a unique auth.users suffix match, then on the group owner calling code. Applied to gdnknuiqxmosuwoytrzc with supabase db push.
- **Interfaces:** New Sources/Manas/Models/PhoneRegion.swift (ISO->calling-code table, dialCodes, deviceDialCode). PhoneIdentity gains canonical(_:signedInAs:), e164(_:defaultDialCode:), maximumDigits=15; normalized is now explicitly the already-international form. New AppStore.canonicalPhone(_:) used by shareGroup/addMember/ShareGroupPopover/MobileShareGroupSheet. SyncSettingsView.normalizedPhone delegates to PhoneIdentity.e164. New supabase/migrations/20260807060000_canonical_member_phones.sql (public.canonical_phone_id, public.canonicalize_member_phone, trigger shared_group_members_canonical_phone). New scripts/rls-check.sh (docker + auth stub, replays all migrations, reproduces the mismatch and proves repair/trigger/boundary). scripts/e2e.sh share_checks gains the live half. New DEBUG seam -manasProbeShareWith <digits> in ios/App/RootView.swift. MemberBadge.initials untouched (sibling lane).
- **Follow-ups:**
  - The e2e test account +1 555 555 0100 is a member of the real "Maanasa apt buy list" group [out of lane] — Added 2026-08-07 05:22 by an earlier debugging session, before this task. It is the account every CI/e2e run signs into, and it can read the apartment list. Not caused by this change and not removed unasked — deleting a member row from the user live group is their call.
  - A wedged or unresolvable share is still silent in the UI [out of lane] — The invite field now says "Add the country code" when it cannot resolve a number, but a member whose sync is failing still sees an empty group with no explanation — the same blind spot that made this bug take a database session to find.
  - Ship the client that matches the migration [out of lane] — The migration is live, so existing rows are repaired and the trigger protects new writes from old clients. But the canonicalizing client itself has not shipped: mac 0.4.5 and TestFlight build 17 still send national digits and rely on the trigger.
  - Countries whose national numbers vary in length (DE, BR, UK) can be refused [out of lane] — The length rule needs the inviter and invitee national numbers to be the same length. When they are not, the derived prefix fails the calling-code check and the invite is refused with "add the country code" rather than stored wrong. Deliberate, but a real UX edge for those regions.
- **By:** n0 · 2026-08-07T06:38:09.268Z

## n0: Phone identity canonicalized to E.164 on client and server;
- **Did:** Phone identity canonicalized to E.164 on client and server; Krithik's live member row repaired and verified
- **By:** n0 · 2026-08-07T06:38:17.554Z

## n0: phone identity is E.164 digits, and the country code is resolved, never assumed
- **Did:** Root-caused "Krithik can't see the group" against the live project rather than from the code. `shared_group_members` held him as `3098264765` while `auth.users` authenticates him as `13098264765`; `is_share_member` compares those with `=`, so RLS correctly hid the group and both of its todos from him, on every launch, while looking complete on the owner's device. `PhoneIdentity.normalized` returned bare digits, so "(309) 826-4765" — how a US number is said out loud — was stored ten digits long, while a number picked from a contact card carrying `+1` was stored eleven. Same roster, two identity schemes. Fixed in three places. (1) `PhoneIdentity.canonical(_:signedInAs:)` resolves a missing country code from the inviter's own identity *by length* — within one country every national number is the same length, so whatever the reference carries in front of a number that long is the calling code — checked against `PhoneRegion.dialCodes` before it is believed, and refused outright when it cannot be resolved, so the UI asks for a `+` instead of storing digits that reach nobody. Every human-entry point goes through `AppStore.canonicalPhone`, which is nil until `currentPhone` arrives (found by running it: the probe raced the first sync pass and produced bare digits). (2) macOS sign-in was doing `"+" + digits`, so a bare US number was sent to the OTP provider as `+3098264765` — a different number; it now fills in the device region's code via `PhoneIdentity.e164`. (3) `supabase/migrations/20260807060000_canonical_member_phones.sql` canonicalizes on the server: a BEFORE INSERT/UPDATE trigger against the caller's identity (so the shipped 0.4.5 clients stop writing broken rows before everyone updates), plus a one-time repair keyed first on a unique `auth.users` suffix match and then on the group owner's calling code. The auth.users branch is repair-only on purpose — run on every client write it would turn a membership row into an oracle for "which registered number ends in these digits?".
- **Interfaces:** New `Sources/Manas/Models/PhoneRegion.swift` (curated ISO→calling-code table; `dialCodes`, `deviceDialCode`). `PhoneIdentity` gains `canonical(_:signedInAs:)`, `e164(_:defaultDialCode:)`, `maximumDigits`; `normalized` is now explicitly the *already-international* form and caps at 15 digits. New `AppStore.canonicalPhone(_:)`, used by `shareGroup`/`addMember` and both share surfaces. `SyncSettingsView.normalizedPhone` delegates to `PhoneIdentity.e164`. New `scripts/rls-check.sh` (docker + auth stub, replays every migration, reproduces the mismatch and proves the repair, trigger and boundary); `scripts/e2e.sh share_checks` gains the live half. New DEBUG seam `-manasProbeShareWith <digits>` in ios/App/RootView.swift.
- **Verified:** migration applied to `gdnknuiqxmosuwoytrzc` with `supabase db push`; his roster row is now `13098264765` and, evaluated with his exact JWT phone claim through the production policies, he reads the group and both todos while a non-member reads nothing. The shipped iOS client, signed in as the test account, turned a typed `4155550137` into `14155550137` on the live server, and the invitee's real token then read the group.
- **Watch out for:** changing the canonical digits changes a member's avatar *colour* (`MemberBadge.paletteIndex` hashes the phone) — a one-time shift, stable afterwards. `MemberBadge.initials` is untouched. And `UserDefaults` parses launch-argument values as old-style plists, so `-manasProbeShareWith "(415) 555-0137"` arrives as an array and reads back nil; pass bare digits, and put bare flags like `-manasTestSignIn` last or they swallow the next argument.
- **By:** n0 · 2026-08-07
## n0: Resolved the single 2-sided jj merge conflict (DECISIONS.md) for
- **Did:** Resolved the single 2-sided jj merge conflict (DECISIONS.md) for the cross-device share-membership fix. The conflict was purely additive log churn: the integrated side had appended the conductor plan-approval entry plus n0's two completion reports, while the worker side appended its n0 rationale section ("phone identity is E.164 digits, and the country code is resolved, never assumed"). Kept both, main content first then the worker section, matching the additive-log pattern used by every earlier DECISIONS.md resolution in this repo (n0/n3/n1/n2/n5). Nothing was dropped from either side. All Swift files auto-merged cleanly. Verified: jj resolve --list reports no conflicts, swift build clean, swift test 314 XCTest tests 0 failures 14 skipped (env-gated) plus 6 swift-testing tests passing, and the worker's new files (Sources/Manas/Models/PhoneRegion.swift, scripts/rls-check.sh, supabase/migrations/20260807060000_canonical_member_phones.sql) all survived the merge with the 10-vs-11-digit regression coverage in Tests/ManasTests/SharedGroupTests.swift passing.
- **Interfaces:** DECISIONS.md only — no code changed. Both the n0 completion reports and the n0 E.164 rationale section are retained in the log.
- **Follow-ups:**
  - Ship the client that matches the applied migration [out of lane] — n0 flagged that the canonicalizing migration is already live on gdnknuiqxmosuwoytrzc, but mac 0.4.5 and TestFlight build 17 still send national digits and rely on the server trigger.
- **By:** n0 · 2026-08-07T06:40:25.654Z

## n1: Member avatars now show initials resolved from the device
- **Did:** Member avatars now show initials resolved from the device address book instead of the last two digits of a phone number, on macOS and iOS. New Sources/Manas/Design/ContactNames.swift (Design/, not Models/, because the widget extension compiles all of Models/ and must not link Contacts) holds a @MainActor @Observable resolver behind a ContactDirectory seam: CNContactStore predicate first, full-enumeration fallback, results cached per identity so a redrawing list reads the store once. Matching reuses n0's PhoneIdentity untouched - ContactMatch.isSamePerson canonicalizes a contact number against the member's OWN identity (PhoneIdentity.canonical(number, signedInAs: identity)), so '(309) 826-4765' matches member 13098264765 with no signed-in number threaded through six call sites, and the reverse direction covers legacy bare member rows. Precedence is contact name > member displayName > unnamed, expressed as the pure MemberBadge.initials(contactName:displayName:). MemberBadge.initials now returns String? and takes only a name; the digits.suffix(2) fallback is gone and MemberAvatar draws a neutral person.fill glyph at the same size, so no row reflows. Contacts stay on device: a resolved name is drawn and forgotten, never written to a member record and never pushed to Supabase. Access is requested only from people-shaped surfaces (share popover/sheet, app root when shared groups exist), never from a list row; denial degrades silently to the glyph.
- **Interfaces:** NEW Sources/Manas/Design/ContactNames.swift: ContactNames (@MainActor @Observable, .shared; name(for:), label(for:signedInAs:fallback:), resolve(_:), requestAccessIfNeeded(for:)); protocol ContactDirectory (canRead/canAsk/requestAccess/names(for:)); SystemContactDirectory; ProbeContactDirectory (MANAS_PROBE_CONTACTS launch seam, values like '(309) 826-4765=Krithik Rao,+1 415 555 0137=Ada Kane' or the literal 'denied'); enum ContactMatch.isSamePerson(_:as:). CHANGED (source-breaking): MemberBadge.initials(name:phone:)->String is now initials(name:)->String? plus initials(contactName:displayName:)->String?; MemberAvatar gained MemberAvatar.Mark and init(mark:tint:size:) (init(initials:tint:size:) and init(member:size:) unchanged at call sites). ADDED NSContactsUsageDescription to ios/project.yml and to the Info.plist scripts/make-app.sh writes - the task brief said this plumbing already existed, but it did not: both contact pickers are deliberately out-of-process precisely to avoid needing it. Reading CNContactStore is a different feature and requires it. PhoneIdentity and the sync/sharing layer were not touched.
- **Follow-ups:**
  - Decide whether the App Store privacy label needs a Contacts entry [out of lane] — The app now reads CNContactStore. Names are matched and drawn on device and never transmitted, which under Apple's rules is not 'collection', so the label should not need to change - but it is a shipping-time judgement worth making explicitly rather than by omission, and the iOS submission is the place it bites.
  - Consider offering the resolved contact name as a one-tap way to set a member's displayName [out of lane] — A resolved name is local only, so the other people in the group still see the member unnamed. The share sheet already has a 'Name' button; prefilling it with the address-book name would let the owner publish it deliberately with one tap, without the app ever uploading contacts on its own.
  - AGENTS.md's screenshot guidance was stale and cost real time [out of lane] — The documented screencapture -x -o -l path has been broken since macOS 15 and fails with a message that reads like a permissions problem. I corrected AGENTS.md, but any other verification script or skill in the wider toolchain that still shells out to screencapture -l is silently broken on this machine.
- **By:** n1 · 2026-08-07T07:12:23.405Z

## n1 — Contacts-backed member initials (avatars)

- Member avatars no longer fall back to `digits.suffix(2)`. `MemberBadge.initials`
  now returns `String?` and takes only a name; `MemberAvatar` draws a neutral
  `person.fill` glyph when there is none. Precedence is
  **device address book → member `displayName` → unnamed glyph**, expressed as the
  pure `MemberBadge.initials(contactName:displayName:)`.
- The lookup lives in the new `Sources/Manas/Design/ContactNames.swift` — Design/
  rather than Models/ because the widget extension compiles all of Models/ and has
  no business linking Contacts. It is `@MainActor @Observable` with a `.shared`
  instance; views read it synchronously and get nil until the answer lands, so a
  todo row never waits on Contacts.
- **Contacts stay on device.** A resolved name is drawn and forgotten — never
  written onto a `SharedGroupMemberRecord`, never pushed to Supabase. `displayName`
  remains the only name that syncs. Anyone touching the sync layer: do not start
  persisting these.
- Matching reuses n0's `PhoneIdentity` unchanged. `ContactMatch.isSamePerson`
  compares a contact number to a member identity by canonicalizing *against the
  member's own identity* (`PhoneIdentity.canonical(number, signedInAs: identity)`),
  which means the resolver needs no signed-in number threaded through it, and the
  reverse direction covers legacy bare member rows.
- Reading `CNContactStore` needed `NSContactsUsageDescription` added to
  `ios/project.yml` and to the plist `scripts/make-app.sh` writes. The existing
  contact *pickers* deliberately avoid that permission and are untouched.
- Access is requested only from people-shaped surfaces (the share popover/sheet,
  and the app root when shared groups exist) — never from a list row.
  `MANAS_PROBE_CONTACTS` is a launch seam holding a fixed address book (or the
  literal `denied`) for verification runs.
## n1: Resolved the single 2-sided jj merge conflict (DECISIONS.md) for
- **Did:** Resolved the single 2-sided jj merge conflict (DECISIONS.md) for the Contacts-backed member initials task (n1). The conflict was purely additive log churn at the end of the file: base was empty at the hunk, the integrated side had appended n1's completion report, and the worker side appended its n1 rationale section ("n1 — Contacts-backed member initials (avatars)"). Kept both, main content first then the worker section, matching the additive-log pattern every earlier DECISIONS.md resolution in this repo used (n0/n3/n1/n2/n5). Nothing dropped from either side. All Swift, project.yml, plist and script files auto-merged cleanly and both lanes' work survived: n0's PhoneIdentity.canonical/e164 + PhoneRegion.swift, and n1's Sources/Manas/Design/ContactNames.swift, the pure MemberBadge.initials(name:)/initials(contactName:displayName:) returning String?, the removed digits.suffix(2) fallback, and NSContactsUsageDescription in ios/project.yml, ios/App/Info.plist and scripts/make-app.sh. Verified: jj resolve --list reports no conflicts, no stray conflict markers anywhere in the tree, swift build clean (one pre-existing unrelated Sendable warning in Sync/TodoRecord.swift), swift test 330 XCTest tests 0 failures 14 env-gated skips plus 6 swift-testing tests passing, including the 15 ContactNamesTests.
- **Interfaces:** DECISIONS.md only — no code changed. Both the n1 completion report and the n1 Contacts-backed-initials rationale section are retained in the log.
- **Follow-ups:**
  - App Store privacy label decision for Contacts [out of lane] — n1 flagged that the app now reads CNContactStore; names are matched and drawn on device and never transmitted, so the label likely needs no change, but it is a shipping-time judgement that should be made explicitly rather than by omission.
  - Ship the client that matches the applied migration [out of lane] — Carried forward from n0: the canonicalizing migration is already live on gdnknuiqxmosuwoytrzc, but mac 0.4.5 and TestFlight build 17 still send national digits and rely on the server trigger.
- **By:** n1 · 2026-08-07T07:14:52.644Z

## n2: Integration pass: both defects confirmed gone by RUNNING both
- **Did:** Integration pass: both defects confirmed gone by RUNNING both apps, not by reading the diff. `bash scripts/e2e.sh` passes end to end (316 unit cases, live backend contract, two-account shared-group RLS boundary, live client integration, iOS app+widget build) — run twice, before and after my changes. macOS: copied dist/Manas.app, stripped SUFeedURL, launched through LaunchServices with MANAS_DISABLE_SYNC=1 / MANAS_STATE_FILE / MANAS_DISABLE_AUTO_CHECKS=1, drove it via the accessibility API and captured window-scoped screenshots with ScreenCaptureKit. Avatars render KR / VD from the address book (and a neutral person glyph, never digits, when there is no contact); typing "(309) 826-4765" into the real share field and pressing the real Share button wrote member phone 13098264765 — the exact identity auth.users holds for that account. iOS: simulator, explicit -derivedDataPath, offline seams, -manasProbeShareWith 3098264765 logged `canonical=13098264765 members=13042164370,13098264765`; screenshot shows KR/VD, and MANAS_PROBE_CONTACTS=denied falls back to the glyph. Two integration fixes were needed to make that possible: (1) ManasIOSApp named StytchSyncAuth() explicitly, bypassing SyncController's env-aware default — MANAS_DISABLE_SYNC had never worked on iOS and every simulator run was hitting the live backend; (2) macOS had no way to be signed in offline, so added MANAS_PROBE_SIGNED_IN_AS to SignedOutSyncAuth (identity without a session; bearerToken() throws and syncNow() now returns early whenever the disable seam is set, so nothing can leave the machine). Migration 20260807060000_canonical_member_phones.sql is confirmed ALREADY APPLIED on gdnknuiqxmosuwoytrzc: trigger enabled, canonical_phone_id('3098264765','14155550137')='13098264765', zero member rows shorter than their group owner's identity, Krithik's real row on 'Maanasa apt buy list' reads 13098264765. Nothing needed re-applying and no live rows were written by my runs (verified). AGENTS.md 'Execute: Discoveries' now carries the canonical E.164 phone-identity rule and the contacts-stay-on-device rule; README.md documents the initials and the Contacts prompt. NOT SHIPPED: no version bump, no notarize, no release, no push, no deploy.
- **Interfaces:** CHANGED Sources/Manas/Sync/SyncAuth.swift: SignedOutSyncAuth now reads MANAS_PROBE_SIGNED_IN_AS — isSignedIn/phone report that number when set, still signed out otherwise; requestCode/verifyCode/bearerToken/deleteAccount still throw. CHANGED Sources/Manas/Sync/SyncController.swift: start(store:) attaches the store and calls publishIdentity() BEFORE the isDisabledByEnvironment guard (so an offline run knows its own number, which is what an invite's missing country code resolves against); syncNow() gained a leading `guard !Self.isDisabledByEnvironment`. CHANGED ios/App/ManasIOSApp.swift: SyncController(auth: StytchSyncAuth(), stateURL:) -> SyncController(stateURL:) so the env-aware default applies on iOS. Docs: AGENTS.md 'Execute: Discoveries' (+4 entries), README.md 'Shared groups'/'Avatars with real initials', DECISIONS.md n2 section. No changes to PhoneIdentity, PhoneRegion, ContactNames, MemberBadge, MemberAvatar, the sharing store, the migration, or scripts.
- **Follow-ups:**
  - Ship the client that matches the already-applied migration [out of lane] — Carried forward from n0/n1 and still true: the canonicalizing trigger is live on gdnknuiqxmosuwoytrzc, but mac 0.4.5 and TestFlight build 17/22 still send national digits and rely on the server to fix them. The release sequence in AGENTS.md is the remaining work; preflight currently BLOCKS on one item — no CI run for HEAD, because nothing is pushed.
  - Decide the App Store privacy label for Contacts [out of lane] — Third time this has been flagged (n1 twice). The app now reads CNContactStore; names are matched and drawn on device and never transmitted, which under Apple's rules is not collection — but it is a shipping-time judgement that should be made explicitly at iOS submission rather than by omission.
  - MANAS_DISABLE_SYNC never worked on iOS — audit other verification runs that assumed it did [out of lane] — ManasIOSApp bypassed the env-aware default, so every past simulator verification run reached the live backend. Fixed here, but any conclusion drawn from an earlier iOS probe run about 'offline' behaviour was measuring the live backend instead.
  - Pin the CI toolchain [out of lane] — preflight warns that macos-26's Swift is not pinned, so a green local build still proves nothing about CI. Pre-existing, unrelated to this node.
- **By:** n2 · 2026-08-07T07:36:08.814Z

## n2 — Integration pass: verifying the sharing fix on a device, not on paper

- 2026-08-07 (n2): the n0 fix could be *run* on iOS (`-manasProbeShareWith`) but
  not on macOS — the share popover needs `sync.isSignedIn` and a
  `store.currentPhone`, and the only way to get either on macOS was to sign the
  verification run into the user's real account through the shared keychain.
  Added `MANAS_PROBE_SIGNED_IN_AS=+1…` to `SignedOutSyncAuth`: it gives a
  scratch run an identity without a session. `bearerToken()` still throws and
  `syncNow()` now returns early whenever `MANAS_DISABLE_SYNC` is set, so the
  seam cannot reach the network. `SyncController.start(store:)` publishes the
  identity before the disabled guard rather than after it — that one line is
  what makes the store know its own number offline.
- 2026-08-07 (n2): `ManasIOSApp` constructed `SyncController(auth:
  StytchSyncAuth(), …)`, naming the auth explicitly and so bypassing
  `SyncController`'s own env-aware default. `MANAS_DISABLE_SYNC` has therefore
  never worked on iOS, and every simulator verification run has been talking to
  the live backend. Now `SyncController(stateURL:)`, which picks the same Stytch
  bridge in an ordinary launch. Anyone adding a platform entry point: let the
  default stand.
- 2026-08-07 (n2): both defects were confirmed by running the apps, not by
  reading the diff. macOS: `dist/Manas.app` copied, `SUFeedURL` stripped,
  launched through LaunchServices with the offline seams, typed
  "(309) 826-4765" into the real share field with `CGEventPostToPid` and pressed
  the real Share button — the member row written was `13098264765`, the identity
  `auth.users` holds for that account. iOS: simulator, offline seams,
  `-manasProbeShareWith 3098264765` → `canonical=13098264765
  members=13042164370,13098264765`. Avatars read "KR"/"VD" on both, and with
  `MANAS_PROBE_CONTACTS=denied` they fall to the neutral glyph rather than to
  any digits.
- 2026-08-07 (n2): the canonicalizing migration
  (`20260807060000_canonical_member_phones.sql`) is **applied** on
  `gdnknuiqxmosuwoytrzc` — trigger `shared_group_members_canonical_phone` is
  enabled, `canonical_phone_id('3098264765','14155550137')` returns
  `13098264765`, and no member row on the project is shorter than its group
  owner's identity. Krithik's real row on "Maanasa apt buy list" reads
  `13098264765`. Nothing needed re-applying.
## n2: Resolved the single 2-sided jj merge conflict (DECISIONS.md) for
- **Did:** Resolved the single 2-sided jj merge conflict (DECISIONS.md) for the n2 integration/release-readiness node. Base was empty at the hunk — both sides were pure appends to the end of the log: the integrated side had n2's completion report, the worker side had the n2 rationale section ("n2 — Integration pass: verifying the sharing fix on a device, not on paper"). Kept both, main content first then the worker section, matching the additive-log pattern every prior DECISIONS.md resolution in this repo used (n0/n3/n1/n2/n5). Nothing dropped from either side; no code touched. All Swift, plist, project.yml and migration files auto-merged cleanly and all three lanes survived: n0 (PhoneIdentity in Models/SharedGroup.swift, Models/PhoneRegion.swift, migration 20260807060000_canonical_member_phones.sql), n1 (Design/ContactNames.swift, NSContactsUsageDescription in ios/project.yml + scripts/make-app.sh, no digits.suffix(2) anywhere in the tree), n2 (MANAS_PROBE_SIGNED_IN_AS in SyncAuth.swift, publishIdentity() before the isDisabledByEnvironment guard in SyncController.start(store:) plus the syncNow() guard, SyncController(stateURL:) in ManasIOSApp.swift, and the AGENTS.md Execute: Discoveries + README.md doc updates). Verified: jj resolve --list reports no conflicts, no stray conflict markers anywhere in the tree, and bash scripts/e2e.sh passes ALL on the merged tree — macOS build, 316 unit cases, live backend contract, the 8-check two-account shared-group RLS boundary (including the two that pin the n0 fix: an invite typed without a country code is stored as the invitee's identity, and the group reaches a member added by their national number), 3 live client integration cases, and the iOS app+widget build.
- **Interfaces:** DECISIONS.md only — no code changed. Both the n2 completion report and the n2 integration rationale section are retained in the log.
- **Follow-ups:**
  - Ship the client that matches the already-applied migration [out of lane] — Carried forward from n0/n1/n2 and still true: the canonicalizing trigger is live on gdnknuiqxmosuwoytrzc, but mac 0.4.5 and TestFlight build 17/22 still send national digits and rely on the server to fix them. Release preflight currently BLOCKS on no CI run for HEAD, because nothing is pushed.
  - Decide the App Store privacy label for Contacts [out of lane] — Flagged three times now (n1 twice, n2 once). The app reads CNContactStore; names are matched and drawn on device and never transmitted, which under Apple rules is not collection — but it is a shipping-time judgement that should be made explicitly at iOS submission rather than by omission.
  - Audit past iOS verification runs that assumed MANAS_DISABLE_SYNC worked [out of lane] — n2 found ManasIOSApp bypassed the env-aware default, so every past simulator verification run reached the live backend. Fixed, but earlier iOS probe conclusions about offline behaviour were measuring the live backend.
- **By:** n2 · 2026-08-07T07:39:17.407Z

## worker: Merged all outstanding work into main and shipped 0.4.6
- **Did:** Merged all outstanding work into main and shipped 0.4.6 to both platforms. The DAG work (n0/n1/n2) was already merged locally; I also found and merged stranded work in a paused workspace that did not compile (it referenced JudgeError.analyticsReason, which had never been written) — judge timeout 180s->900s plus a coarse failure reason in analytics, completed with two tests. e2e green locally and in CI on the exact shipped commit. macOS 0.4.6 notarized, published, appcast signed and live; iOS build 23 uploaded, attached to the external group, beta-reviewed and APPROVED. Also closed a real gap: nothing in the repo attached a build to the external TestFlight group, so a release could look complete while reaching zero external testers — added tf_external and tf_delivery_status lanes and wrote the sequence into AGENTS.md.
- **Interfaces:** JudgeError.analyticsReason -> String: fixed vocabulary of constants, never interpolates the error payload. UsageAnalytics.Event.checkInFailed(automatic:reason:) — gained the reason parameter. AppStore.judgeTimeout = 900 (was 180). fastlane tf_external (TF_BUILD_NUMBER) — attach a build to the External Testers group. fastlane tf_delivery_status (TF_BUILD_NUMBER) — report the processing/internal/external/betaReview chain.
- **Delivery:** deployed; release; target=Manas 0.4.6 — GitHub release + notarized DMG, Sparkle appcast on manas.viraat.dev, and iOS build 23 on TestFlight (internal + external); revision=ffaaabd3ca101a106b3beca7d2ba4443a66505fb; verifiedAt=2026-08-07T18:59:23Z; checks=12
- **By:** worker · 2026-08-07T19:01:01.880Z

## worker: Fixed the share panel showing phone numbers instead of
- **Did:** Fixed the share panel showing phone numbers instead of names, then shipped it as 0.4.7 to both platforms. Root cause: the avatar and the label beside it were two code paths and only MemberBadge.initials consulted the address book — AppStore.memberLabel went straight from displayName to the digits, so a shared group drew KR in the circle and +1 (309) 826-4765 in the text next to it, with a Name button offering to type a name the phone already knew. memberLabel now runs the badges precedence (you, address book, group displayName, number) and AppStore.hasName gates every naming affordance. Privacy shape unchanged: resolved names are drawn and forgotten, never written to a member record or pushed to Supabase. Verified by RUNNING it — drove a release bundle through the accessibility API with MANAS_PROBE_CONTACTS seeded and read Krithik Rao over the number with no Name button, plus a contacts-denied control that correctly falls back to digits. Also fixed a second silent verification hole found while doing that: ManasIOSApp named AppGroup.stateURL explicitly, bypassing the MANAS_STATE_FILE override, so the scratch-state seam was macOS-only and simulator runs read and wrote the real state file.
- **Interfaces:** AppStore.memberLabel(_:) — now resolves the address book first; every member-rendering site must go through it rather than displayName ?? digits. AppStore.hasName(_:) -> Bool — what any name this person affordance keys on. AppStore.contactNames: ContactNames — injectable resolver, defaults to .shared, so the naming rules are testable. AppGroup no longer vends stateURL; construct the iOS store as AppStore() so MANAS_STATE_FILE is honoured.
- **Delivery:** deployed; release; target=Manas 0.4.7 — GitHub release + notarized DMG, Sparkle appcast on manas.viraat.dev, /Applications/Manas.app, and iOS build 24 on TestFlight (internal + external); revision=cab1452b7e31; verifiedAt=2026-08-09T22:50:05Z; checks=15
- **By:** worker · 2026-08-09T22:50:44.746Z

## worker: Fixed the three things wrong with the share panel
- **Did:** Fixed the three things wrong with the share panel and shipped 0.4.8 to both platforms. (1) Duplicate numbers: when nobody has a name, memberLabel falls back to the number and both views drew the number AGAIN as the caption — iOS unconditionally, macOS for any owner. Both now render one shared AppStore.presentation(of:in:) whose detail line is derived from the title rather than guessed alongside it, so it structurally cannot repeat it. (2) Not knowing whether a name syncs: a contacts name is local and never pushed, a typed displayName reaches everyone, and on the row they looked identical. nameSource now distinguishes them, contacts-derived names carry a person.crop.circle marker, and both panels state the rule in a footer. (3) Naming yourself went through setMemberName, which writes one membership row — so your name was missing from your other groups and Settings still showed an empty field, which reads exactly like a name that failed to sync. Your own row now routes to setMyDisplayName. Verified by driving a release bundle through the accessibility API with real key events: all four member cases render one number each, and typing a name into your own row lands it in myDisplayName and on the pushable membership row while the contacts-named member stays null.
- **Interfaces:** AppStore.presentation(of:in:) -> MemberPresentation (title, detail, nameSource) — the single way both platforms render a members-list row; do not reintroduce a per-platform caption. AppStore.MemberNameSource: .you / .contacts (local, never pushed) / .group (synced displayName) / .none. AppStore.nameSource(of:) -> MemberNameSource. Naming yourself must call setMyDisplayName, never setMemberName.
- **Delivery:** deployed; release; target=Manas 0.4.8 — GitHub release + notarized DMG, Sparkle appcast on manas.viraat.dev, /Applications/Manas.app, and iOS build 25 on TestFlight (internal + external); revision=80a7a52f1f74c264348c4f280d6812b0d40719c3; verifiedAt=2026-08-10T00:57:37Z; checks=14
- **By:** worker · 2026-08-10T00:58:16.293Z

## worker: Removed the stray member the user asked about, corrected
- **Did:** Removed the stray member the user asked about, corrected a misunderstanding I caused, and shipped 0.4.9. Ada Kane and +1 (650) 555-0188 were never in the users app — they were fixtures I seeded into a scratch state file during verification and then presented in a summary table without saying they were synthetic. The real stray was +1 (555) 555-0100, the beta test account, genuinely a member of Maanasa apt buy list on the live backend, almost certainly added by a past verification run back when MANAS_DISABLE_SYNC and MANAS_STATE_FILE were silently macOS-only. Removed it through the apps own UI so it wrote a proper tombstone and synced, then verified from the removed accounts own session that it now sees zero groups and zero membership rows. While in there, found the bigger problem: the Mac has NO kTCCServiceAddressBook row for dev.viraat.manas at all, so the contacts-naming feature shipped in 0.4.6/0.4.7 has been inert on the users machine with nothing on screen admitting it. 0.4.9 makes that state visible and fixable and stops the app burning its one permission ask on a prompt that never appeared.
- **Interfaces:** ContactNames.canReadContacts / .canAskForContacts — what a members list keys its no-permission explanation off. requestAccessIfNeeded now sets hasAskedForAccess from !directory.canAsk AFTER awaiting, so an unshown prompt is retried rather than swallowed.
- **Delivery:** deployed; release; target=Manas 0.4.9 — GitHub release + notarized DMG, Sparkle appcast on manas.viraat.dev, /Applications/Manas.app, and iOS build 26 on TestFlight (internal + external); revision=05d8c27e388a313b7074cafd87b7f581c4659c8b; verifiedAt=2026-08-10T03:05:12Z; checks=15
- **By:** worker · 2026-08-10T03:05:45.143Z

