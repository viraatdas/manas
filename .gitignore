# Rudder
.rudder/
.rudder-worktrees/
RUDDER.md
RUDDER_SHARED.md
.claude/skills/rudder-*/

# Swift Package Manager / Xcode
.build/
dist/
.swiftpm/
DerivedData/
*.xcuserstate

# macOS
.DS_Store
.vercel

# Release-time config shared by the mac and iOS build scripts (analytics
# token). Gitignored: the PostHog project token is write-only and public-ish,
# but it still doesn't belong in the repo.
release.env
